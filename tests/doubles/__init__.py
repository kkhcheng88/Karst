"""測試替身(test doubles):**只放替身,不放斷言**。

替身是可換件的證據——引擎換走,策略層一個字不用改(D-007 第 3 條)。以前每個
測試檔各寫一個假引擎,四份幾乎一樣的碼各自飄開:改一次引擎合約要四處跟改,
而漏改的那一份會靜靜地繼續過測試。KARST-090 收成一份。

**這裡沒有假定義庫。** 測試照舊起一個臨時 sqlite 真庫、照舊經唯一入口寫
(D-020 第 4 條)——庫的行為(版本鏈、寫入者簽章、查重)正是要驗的東西,
造一個假的出來就等於不驗。
"""
