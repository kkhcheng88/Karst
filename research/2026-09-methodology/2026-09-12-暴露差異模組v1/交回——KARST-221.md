# 交回——KARST-221

**結論**:`basket_core.py` 市值三處改碼齊(未除息還原收市價、封面股數乘回拆股比率、缺股數標 NaN 加 `mcap_status`),與 KARST-219 參考實作逐行一致、單元個案零誤差。但 199 的「估值平者勝」修後不再成立——同 SIC 籃子市銷率事件內 AUC 0.404→0.487、市帳率 0.418→0.518,兩條都由「分得開」退回「分不開」;貴者中位超額 −17.3%→−8.8%,單調消失。已舉手問 199 應否改讀,票留開等收件箱。

**產物**:`basket_core.py`(三處)、`verify_mcap_patch.py`＋`mcap修正/驗證——生產者碼對參考值.txt`、`對照——市值修正後兩條特徵.md` 與五份 `out/*_mcap修正.csv`(原檔未改)、六檔總覽尾各加一行標註;兩支腳本可重跑。

**未解**:199 改讀待人答;修後仍有約 1,100 行取不到市值(面板無股數或過期),未補外部股數。
