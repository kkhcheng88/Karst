# 主考試執行備忘(主 agent;2026-09-13 起滾動記;不改任何凍結件)

## 進度

- DeepSeek 臂(KARST-233):首批 12 宗落檔並核(csv 12/12;引用 33 正確 / 1 錯引 / 1 捏造);餘下 72 宗兩隊在跑(第 11–42、第 45–84)。
- Opus 臂(KARST-223):W1(E001–E012)四隊 05:45 派出;E010–E012 隊 06:1x 交(判 低/低/中);其餘三隊待。波次表在 scratchpad `opus-waves.md`:W2 E013–E024 … W7 E073–E084。
- 最終 84 = E001–E084(E056/E067/E070 三槽由補位公司重建包)。

## 判斷者交低的取證包缺陷(記錄,本場不改;票 D 評分與 v2 建包參考)

1. 同業欄按 SIC 四位數抓到不同生意或不同生命期的公司(多宗);供給側「投資」組因此常為查不到。
2. 建包的「稿內數字比對」只認損益表主數,EX-99.1 資產負債表已印的現金/存貨/應收/債務被標查不到(E01x 一宗);判斷者按「稿內已公布可用」自行引用。
3. EX-99.1 抽文(`ex991_full_text`)會吃掉指引數字表(E012):全年指引只剩引言句,數字由同日逐字稿補回;**評分以 C1 為對照時要留意稿內指引可能缺表**——票 D 核 C1 時對照原稿 HTML。
4. E010 包側 capex 標「稿內」但稿內無(KARST-234 發現)。
5. E003(DeepSeek)pred 欄以百分比寫(71.0);評分時 |值| > 5 者除以 100,記為尺度違規。
6. 首三宗 DeepSeek 卡的開始時間戳不可信(與結束僅差 34–91 秒)。

7. Opus E001–E003 隊交低:E001 三位數增速全是收購算術(有機 −2.7%)、E003 兩季預測 +5,150%(一筆 3,500 萬預付款對 16 萬基數)——**評分事前補寫(在開結果前,2026-09-13 06:2x)**:MAE 平均另報截尾版(預測與實際皆截尾於 ±200%),主數仍為中位;另加 `driver_acq`(top_driver_type = 收購)與 `g0 > 100%` 兩個分組;此規則寫在此處即算事前。
8. 記憶污染自報:Opus 有一宗自陳「憑記憶多加了信心」(0.88 對乾淨估計 0.75–0.80);評分時把 contamination_note 標「有影響」的卡另分一組報。

9. **取證包數列複製錯(Opus E004–E006 隊查出,主 agent 全掃 84 包確認)**:E006 的 2014Q4 收入與毛利與 2013Q4 完全相同(財年末季推算行出錯),E050 的 2020 年末季同樣與 2019 年末季相同;兩宗的 `prev_q_yoy`/`accel_pp` 不可信,「加速」入池理由可能不成立。**處理(事前寫,2026-09-13 06:5x,兩宗結果未開)**:不換位、不重判(E006 兩臂已判);評分標 `series_defect`,主表報「含」與「剔除兩宗」兩版;下一版建包加「相鄰年度同季收入完全相同」與「四季合計對年報」兩道對帳。
10. Opus E004 隊:訊號季 10-Q 申報日早於 T1(T1 前一日),用了其合約條款與分項歸因,未用 XBRL 訊號季行;評分者留意屬體例差異。一隊收工核對腳本掃到共用目錄其他事件的檔名與 csv 欄位(未開卡,發生在自己三卡定稿後),照實報備。
11. W1 Opus 12 宗全部落檔(06:5x)。
12. **E026(Opus 隊查出)**:包內 g0 分母(XBRL 去年同季)比稿內自印的去年同季高約一成,按稿內口徑訊號季是跌不是加速;入口為假訊號。**與 E006、E050 同列 `entry_defect` 組**,評分主表報含/剔除兩版;不換位不重判。
13. E017(Opus):停業業務重分類令包內 g0 與同口徑相差 18.9pp;E032:XBRL 首報含已出售業務未重列,與可比基數差約 29pp——評分時 M1 的實際值要用與 g0 同口徑的首報值(維持 v1 規則),另在備註列重列口徑差。
14. 同業欄:多隊報「兩大同業」不在真正競爭集(SIC 四位數仍無用,或零家);供給側投資組多為查不到。下一版建包改用年報「Competition」節自述對手名單。
15. 多隊報「改善在盈利/毛利不在收入」或「增速主要來自併購」:評分加 `improve_where`(收入/盈利/組合)與 `driver_acq` 分組(第 7 項已列)。

16. **疑數值越界(Opus E022–E024 隊,07:4x)**:E022、E024 的 `revenue` 欄整組與 T1 前申報不符,疑向後錯位四季(即含 T1 後季度的實際收入),毛利/營業利潤/淨利正確;masking_check 只掃日期抓不到。**KARST-235 緊急核查全部 84 包(companyfacts 重算 + T1 後四季實際值比對)在跑;新 Opus 波次暫停派(E058–E084 未派),已派的 8 隊照跑。** 若核出系統性錯位:受影響包重建、該事件兩臂重判(記為版本差異,不合併算);若只個別包:入 `entry_defect`/`leak_suspect` 組分兩版報。E024 卡有判斷次序污染(先判低後見錯位改中),未污染前值在 contamination_note,評分用前值另跑一次。

17. **KARST-235 結果(08:0x)**:84 包 75 合格;**真越界 2 包(E022、E024:revenue 欄整欄向後錯位一年)**;E017 營業利潤一格為 T1 後重述值;6 包訊號季行標稿內卻放 XBRL 值(E021、E022、E025、E047、E057、E073);tag 口徑差 5 格不停;成因 `finlib.quarterly` 跨 tag 取次序優先。**裁(主 agent)**:不是系統性,Opus 餘下波次恢復派;KARST-236 修九包(三包全數列重建、五包訊號季行、finlib_fixed);**E017、E022、E024 兩臂判斷作廢,包修正後重判**(舊卡改名加「作廢-」前綴保留;新卡記「包修正版」);其餘六包訊號季行修正後不重判(判斷者已按規則只用稿內數字);評分主表另報「剔除九包」版。
18. 提示詞/共同指令不改;修正的是資料層。

19. **DeepSeek 第一隊(E011–E042)收工(08:3x)**:32 宗齊;報包內 `ticker`/`name` 欄 8 包(E005、E011、E024、E036、E039、E060、E071、E082)與稿頭交易所標籤不符,疑取自申報日後的公司身分(前視通道,可能讓模型認出後來的名字);本場不改包;評分時這 8 宗標 `identity_leak`;下一版建包用申報時的名稱與代號。E039/E040 時間戳最初為估算後更正(判詞不變)。E042 記憶方向與判詞同向,核查者優先抽。
20. E050(Opus):訊號季 g0 恰為零、門檻塌成零——正是年末季複製缺陷的後果,已在 `entry_defect` 組;另一端 g0 > 60% 門檻抬到五成——評分按 g0 分組(第 7 項 g0 > 100% 組改為三檔:g0 ≤ 0 / 0–0.3 / > 0.3)。

21. E038(Opus):包內 g0 44.0% 因新舊收入準則口徑斷層被誇大,稿內同口徑 21.2%;E039 prev_q_yoy 與包內數列算出的對不上。KARST-235 把 E025/E033/E038/E039/E047 列「tag 口徑差(T1 前已公開)不停」——**評分補寫**:這五宗的 g0 與門檻另以「稿內同口徑」重算一版並列報(`g0_scope_alt`);票 D 對照兩版。
22. Opus E037–E039 隊自評三宗全高、疑入口甲本身偏選「已鎖定未來收入」的公司——評分報 Opus 臂與 DeepSeek 臂各自的判級分佈與延續實際率的校準表(可靠性圖)。

23. E057(Opus):包內 g0 用「Total Revenues and Other Income」而八季 quarters 用「Sales and other operating revenues」,差 1.9pp;**票 D 評分:M1 與門檻必須同一口徑,一律以 quarters 欄的收入定義重算 g0 作門檻**(列 `g0_quarters`),包頭 g0 只作參考;商品型(能源/原材料)與經營型分組報。E056 取證包 event_id=B028、slot=E056,輸出用 E056。逐字稿 has_qna 旗標不可靠(E057 標 false 實有問答)。
24. 主 agent 由子代理回報的檔名見到三個代號(E055–E057 隊在最終訊息貼了完整路徑);記錄,不作任何處置;之後派工訊息加「最終訊息不貼含代號的路徑」。

25. E053(Opus):包內 ticker 為 T1 後才出現的代號(合併後),已洩漏合併結果——併入第 19 項 `identity_leak` 組(E053 加入)。E054(Opus):執行紀錄誤列一份未讀年報並寫了一句未查證佐證(不支撐任何數字)——按 v1.1「引用文件裡沒有的事實整卡作廢」字面應作廢;**主 agent 裁:該句不入任何判斷格與機械版,卡保留、標 `citation_flaw`,核查票 KARST-234 對此宗全數出處核**;不重判。
26. 多隊指出:訊號季增速極低(g0 ≈ 0)或極高(g0 > 60%)時,總判由門檻算術而非判斷決定;疫情谷底訊號季的四季指標幾乎不含判斷資訊——評分表加「門檻決定型」標籤:g0 ≤ 0.02 或 g0 ≥ 0.6 或訊號季落 2020Q2–2021Q2(基數期),另組報。
27. Opus 全部 84 宗已派(09:0x);E017/E022/E024 待 KARST-236 修包後兩臂重判。

28. E064(Opus):「加速 58.7pp」是分拆業務改列終止經營後基期口徑不一致的假象,T1 前 10-Q 重列後實為減速——併入 `entry_defect` 組(第四宗)。E065:包內 g0 把稿內年份「2023」誤讀成收入(20.23 億 vs 實際 20.199 億),差 0.2pp 不影響總判——記入建包缺陷(數字匹配把年份當數)。E066:has_qna 標 false 實有問答(同 E057)。
29. **用戶 2026-09-13 09:5x 指示:先停手,聽指示;重整根基(清理資料夾、Kira 重置、資料來源盤點)。** 背景已派的判斷讓其自然完成;不派新工;評分是否進行待用戶答。

30. **KARST-236 修包收檔(10:1x)**:E017/E022/E024 全數列重建、五包訊號季行改稿內;84 包重核 leak 0、遮罩 0、75 包 sha 不變;**E002 修正後加速 < 2pp**(入口不成立,併 `entry_defect`);**60 包的包頭 g0/prev/accel 與修正真值重算不符**——票 D 一律以 quarters 欄重算 g0(第 23 項)並列包頭值對照;E011/E016/E048/E080 四包有「單季事實首見於 T1 後申報但值相符」格,不算越界。E017/E022/E024 兩臂重判已派(DeepSeek 10:2x;Opus 隨後)。
31. **用戶 2026-09-13 裁重整根基**:tag `pre-reset-2026-09-13` 已打;step 1–2a 已做(快取、空目錄、第一代引擎、docs、pyproject 移出工作樹);step 2b(①研究目錄與封存刪除)待考試收尾後;step 3 Kira 重置(新 Epic「根基重整」聚焦資料與策略;舊票關;決策簿改為只留現行有效的精簡版,舊版在 tag);step 4 資料盤點。

32. E074(Opus):包內 `prior_release_guidance` 標查不到,但所列上一稿本體其實有指引句(解析漏抓)→ 判斷者誤以為首次給指引;E075:訊號季收入被一筆一次性款項灌水且同稿已下調前瞻指引——**「同一份業績稿有沒有下調前瞻指引」是 T1 前可得的機械檢查,現行入口未用**(下一版入口候選)。評分:對 84 宗重掃上一稿指引句(票 D 順帶,只作標籤)。

33. E084(Opus):包內 g0 −2.5% 對申報 +50.2%——公司訊號季前出售分部列終止經營,XBRL 首報值 2023 各季與 2024Q1 是重列前總額、2024Q2 起是持續經營數,包內以持續經營比總額;判斷者改用申報口徑。**凡有分拆/出售/終止經營的公司同一支腳本都會錯配**——評分:票 D 先掃 84 包「相鄰季收入級距跳變 > 25% 且年報有 discontinued operations」標 `scope_break`,此組另報;M1 實際值必須與門檻同口徑。E080:一個歷史季度收入與 T1 前申報衝突(判斷者取申報)。E082/E084:主要驅動為收購未滿十二個月的機械增量(兩季高、四季低)——`driver_acq` 組。E069/E067:g0 為負或低基期令門檻近乎免費——「門檻決定型」組。
34. **DeepSeek 臂 84/84 落檔(含三宗包修正版重判,11:1x);Opus 主 84 已派完,E017/E022/E024 重判在跑。** 判級分佈(機械檢查):DeepSeek 高 27 / 中 26 / 低 31;Opus(81 宗)高 35 / 中 13 / 低 33;E003 兩臂 pred 欄尺度違規(百分比)。

35. **兩臂 84/84 落檔(11:3x)**。Opus 判級 高 36 / 中 15 / 低 33;DeepSeek 高 27 / 中 26 / 低 31。E017 修正版包仍有:訊號季營業利潤正負顛倒(純文字抽取掉括號)、g0 分子持續經營對分母舊口徑(−8.3% 對同口徑 +10.5%)——評分以 g0_quarters 重算並列 `scope_break`;E024 預測期增長主力為訊號季後完成的收購(`driver_acq`)。逐字稿段號比實際 paragraph_number 少一(核查者發現,12 條),回查要減一。
36. 已派:Opus 全批引用核查(KARST-234 第四批)、票 D 評分(KARST-237);DS 第三批核查在跑。收到評分後主 agent 寫結論,對照執行口徑 v1 第六節與 v1.2 事前決定表。

## 評分前要做

- 合併 B/ds/rows 與 B/opus/rows 各 84 行;KARST-234 滾動核查每臂餘下批次;票 D:兩臂對 C1/C2/C3、g0<0 分組、有無收入指引分組、有無逐字稿分組;事前決定表照執行口徑 v1.2「判斷層雙臂」。
