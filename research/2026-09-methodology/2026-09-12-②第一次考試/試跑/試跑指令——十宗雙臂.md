# 十宗試跑指令(兩臂共用;2026-09-13;票 KARST-227)

> 用戶 2026-09-13 決定:先以十宗比較兩個模型的行為,取得觀察後再定 84 宗主考試的模型安排。**本試跑只觀察模型行為(格式、引用、漏答、讀了什麼、一致性、時間費用),不開任何結果、不比較準確度;輸出不入主考試。**

## 與共同指令的差別(其餘一律照 `B/子代理共同指令——票B.md` 與 `strategy/specs/提示詞——改善驅動可持續性判斷-v1.md`)

1. **取證包改讀** `C:/projects/Karst/research/2026-09-methodology/2026-09-12-②第一次考試/A2/packets/<event_id>.json`(A2 是已作廢的 v1.1 版本,本試跑只借用其取證包;主考試 84 宗另由 A3 建)。包內 `3_截止前文件` 的 `local_gz` 在 `A/edgar_cache/`。
2. **輸出目錄**:DeepSeek 臂寫 `試跑/ds/`(卡 `試跑/ds/卡-<event_id>-<ticker>-<signal_date>.md`,行 `試跑/ds/rows/<event_id>.csv`);Opus 臂寫 `試跑/opus/`(同構)。**不得讀另一臂目錄。**
3. **十個事件**(兩臂同一批):E001、E009、E017、E025、E033、E041、E049、E057、E065、E073。
4. **每事件多回報一段**(寫在卡末「執行紀錄」節):讀了哪些本地文件(accession 與讀的節)、沒讀到的、開始與結束時間、有沒有任何一步偏離提示詞 v1。
5. 硬界線照共同指令:只用取證包與包內列出的本地文件;禁網;禁讀 `controls_operating.csv`、`population*.csv`、`entry_pool.csv`、`thresholds.md`、`picks_before_results.md`、`cache/`、`執行紀錄——*.md`、`data/`;每事件一次執行,寫完不改;`contamination_note` 必填;不寫票檔;不 commit。
