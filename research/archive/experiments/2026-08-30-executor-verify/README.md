# 策略執行台搬遷驗證(KARST-090)

驗什麼:把因子混合搬入策略合約與策略執行台之後,庫內 13 條 `origin='formal'` 的正式運行會不會變——身份層逐條重算運行編號與指紋,內容層再把因子混合那條正式運行(`run-024df83fb4891c89`)繞過查重真的重跑一次引擎,逐日淨值、逐日持倉與逐筆訂單逐位對照。

怎樣跑:`python verify_090.py`(全程只讀,先把資料庫複製一份出來才驗,一句都不寫回倉),結果覆蓋輸出到同目錄的 `verify_090.json`。

結論:13/13 運行編號與指紋逐位相同,零條變號;因子混合那條連 2929 列淨值、持倉與 188 筆訂單亦逐位相同;八項成績指標 11 條算得出且全部與落檔相同,餘下 2 條(`run-f4c162e5aac34347`、`run-728a01087531258f`)的 parquet 序列早在 KARST-057 重建 `data/` 時已不在倉裡,屬既有狀況,與本次搬遷無關。
