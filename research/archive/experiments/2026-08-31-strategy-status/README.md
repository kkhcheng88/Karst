# KARST-117 生產庫核對:因子混合標封存

`verify_117.py`(全程唯讀,兩個庫都以 `mode=ro` 開)對住動庫之前那份備份
`C:\Users\Kaho\.claude\backups\karst.sqlite.2026-08-31.bak` 逐格核,結果落
`verify_117.json`。

小庫測試(`tests/test_strategy_status.py`)證得到閘擋得住、清單過濾得到;證不到的是
真庫那 8,318 次運行、7,096 個參數集、51,848 列既有簽章有沒有被這次封存動過。這一支就是
那一項的證明。

## 四項證據(全部逐格對,不靠雜湊)

| 項 | 結果 |
|---|---|
| 運行逐位不變 | `backtest_run` 8,318 條逐條逐格比,**零格出入**(狀態走旁表、不進運行編號) |
| 既有簽章一個不動 | 51,848 → 51,849 列;零消失、零改動;新增剛好一列 `strategy_status[1\|1]` |
| 封存那條線的家當照留 | 因子混合的策略登記 1、版本 2、參數集 **7,096**、參數取值 28,408、該線運行 **7,094**,全部**零格出入**、照查得到 |
| 狀態與清單 | 封存那一筆依據指明 D-055;預設清單只列兩條現役,加旗標才列到因子混合 |

`karst verify` 三類全部清白:定義 50,830 列、因子批次 997 列、快照 22 列。

## 跑法

```
PYTHONUTF8=1 python experiments/2026-08-31-strategy-status/verify_117.py
```

回傳碼 0 = 全部對得上,3 = 有出入(清單印在 `verdict`)。
