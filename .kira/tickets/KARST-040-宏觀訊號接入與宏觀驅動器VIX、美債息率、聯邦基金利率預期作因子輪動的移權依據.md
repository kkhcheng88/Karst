---
id: KARST-040
title: 宏觀訊號接入與宏觀驅動器:VIX、美債息率、聯邦基金利率預期作因子輪動的移權依據
type: task
createdAt: 2026-08-28
risk: medium
model: opus
fits: 一程
approvalRequired: false
dependsOn: [KARST-036, KARST-027]
claimedBy: null
epic: V1 建置
deliverable: KARST-D02
closed: 2026-08-28
---

## 工作內容

因子輪動的驅動器自此可以用價格以外的宏觀訊號:VIX、美國國債息率(2/5/10/30 年與曲線斜度)、聯邦基金利率與市場預期(FedWatch 的免費替代:聯邦基金期貨或 FRED 有效利率/預期序列),按知情時間接入同一套數據快照,並以矩陣式掃描這些宏觀驅動器對四隻因子 ETF 的移權效果,與 KARST-036 的價格驅動器同一張成績表比較。用戶 2026-08-28 原話「if the quant factor like qlib is not enough, you can even consider to introduce VIX, FedWatch, 美元國債 or else which could have a correlations」。範圍:一個宏觀來源適配器(yfinance 指數代號或 FRED,免費優先)、宏觀序列入快照、至少三個宏觀驅動器、報告。不裁取值。

## 驗收條件

- [x] VIX 與至少三條美債息率序列經來源適配器入數據快照,知情時間處置寫明(收市後可得),快照編號可引用
- [x] FedWatch 的免費替代來源列明並接入(或明文寫出為何暫不可得),不用付費數據
- [x] 至少三個宏觀驅動器各連參數格出穩健平原報告,與價格驅動器及固定權重對照格同表比較,分三段時期列對 SPY 超額
- [x] 驅動器仍是策略層可換件,引擎與目標比重路徑不改

## 結果

· 2026-08-28 09:53 宏觀快照 `2026-08-28-3b2de5c59740` 凍結落檔(14 條序列、41,006 行、來源 `yfinance-macro`),對齊價格快照 `2026-08-27-91a5d51339d9` 的主日曆,期間 2015-01-02 ~ 2026-08-26 共 2,929 個交易日。第一層四件事全部免費、無 API 鑰匙接入:VIX 用 `^VIX`/`^VIX3M`;信用利差用 `HYG`/`LQD` 比值代替 FRED 的 OAS(OAS 要鑰匙);美債息率用 `^IRX`/`^FVX`/`^TNX`/`^TYX`,曲線斜度以 10 年減 3 個月(3 個月替代 2 年,斜度絕對水位因而與市場慣講的 10y−2y 不同,方向一致);FedWatch 本身不免費,改用聯邦基金期貨 `ZQ=F`。第二層(`DX-Y.NYB`、`CL=F`、`IWF`/`IWD`、`IWM`)照裁定只入成績表作對照,不開參數格。非可投資序列處置寫在 `karst/data/macro.py`:宏觀序列沒有 CIK、不入實體表,自成一種快照(`data/macro_snapshots/<編號>/`)以序列代號為主鍵,經 `store.register_snapshot` 登記,拿到同一套可引用編號;宏觀代號從不進價格面板,「不小心持有 VIX」在結構上寫不出來。知情時間一律當收市後才知情,決策日只見當日收市及以前的讀數,可執行點為下一根 bar 開市(D-021);對齊主日曆後最多前填 3 日,超過留空,驅動器見空即拋 `InsufficientHistory`,不當零。六個宏觀驅動器各 30 格(5 門檻或回望 × 3 傾斜 × 2 節奏),共 180 格,連成本(每股 0.005 + 滑點 5bp),目標 `annual_excess:SPY`。最優格與鄰域平均:曲線斜度變化方向 `lookback_days=20、tilt=1、monthly` +4.6641%(鄰域 +1.2139%,孤峰,`run-a3d8dd0ad9cd2b78`),其鄰域平均最高格 `lookback_days=40` +3.0270%(鄰域 +1.7491%);聯邦基金利率預期方向 `lookback_days=10、tilt=1、quarterly` +2.6069%(鄰域 −1.2690%,孤峰);10 年息率趨勢 `lookback_days=20、tilt=1、monthly` +2.3074%(鄰域 −1.0077%,孤峰);VIX 水平開關 `threshold=16、tilt=1、monthly` +0.6806%(鄰域 −0.6050%,孤峰);VIX 期限結構開關 `threshold=1.1、tilt=1、monthly` +0.5813%(鄰域 +0.2861%,**平原**——KARST-036 與 KARST-043 掃遍所有價格驅動器一個平原都沒有,這是本計劃第一個,原因是它一年只講幾次話、其餘時間維持基準權重);信用利差變化方向 −0.5574%(鄰域 −2.1089%,孤峰,唯一輸錢,而 HYG/LQD 比值混入存續期與流動性溢價,故只判得起這個替代品、判不起信用利差本身)。分段對 SPY 超額(2015–2019 / 2020–2022 / 2023–2026):曲線斜度 +1.49% / +5.73% / +8.59%,聯邦基金預期 +2.87% / +3.28% / +2.18%——全表唯二三段皆正而不靠單一時期撐起的條目;10 年息率 −1.22% / +2.85% / +7.34%;VIX 期限結構 +1.77% / −1.66% / +1.54%;VIX 水平 +4.20% / −2.01% / −1.87%;信用利差 −0.60% / +0.11% / −0.78%。差距:曲線斜度最優格 +4.6641% 越過最好的價格驅動器相對強弱 +4.1712%(+0.49pp)與因子動量 +3.3166%(+1.35pp),對固定權重 95/5 季度 +1.2005% 為 +3.46pp、對各佔 25% 季度 −0.6748% 為 +5.34pp;但代價是最大回撤 −33.77%(相對強弱 −18.86%)與換手 5.72(相對強弱 2.51),而且相對強弱的全期超額有一半以上來自 2020–2022 一段(+10.54%),利率驅動器則分佈得平均——所以判得起「利率資訊有增量」,判不起「宏觀應該取代價格驅動器」。價格與固定權重那 6 行是讀回 KARST-036/043 既有運行(運行指紋相同),沒有重算。第二層相關度(對動能減低波的月變化):成長 ETF +0.501、小型股 +0.371、價值 ETF +0.195、油價 −0.021、美元指數 −0.084;相關度高的兩條都是股票價格、與因子輪動同源,不算獨立資訊。報告路徑:`experiments/2026-08-28-macro-drivers/README.md`,數據在 `experiments/2026-08-28-macro-drivers/results/`(成績表.csv 18 行、分段超額.csv、宏觀序列齊全度.csv、第二層對照.csv、summary.json,另每個驅動器一份報告.md/掃描表.csv/判讀表.csv 與三張投影圖)。能力本體:`karst/data/macro.py`(來源適配器與宏觀快照)、`karst/strategies/factor_rotation.py`(`DriverView.macro` 與六個宏觀驅動器)、`karst/sweep/factor_rotation.py`(宏觀快照編號入參數集,故換數據必換運行編號)。引擎、目標比重路徑、`RotationDriver` 公開簽名一律沒動,新參數全部有預設值,四個價格驅動器一行沒改。測試 16 條全綠(`tests/test_macro_snapshot.py` 7、`tests/test_macro_drivers.py` 6、`tests/test_macro_yfinance.py` 3,最後一份離線自動 skip),連同受影響的 `tests/test_factor_rotation.py`、`tests/test_engine_costs.py`、`tests/test_data_snapshot.py` 合共 41 條全綠。沒有新增依賴,`pyproject.toml` 未動。

## 留言

· 2026-08-28 09:53 KARST-040-macro:`tests/test_factor_rotation.py` 有一條斷言原本寫死 `EXTERNAL_DATA == ()`,本票把它填了四行(宏觀序列的來源與知情時間)故必然失效。該條測試的真正用意是「四個價格驅動器不需要外部數據」,這一點仍然成立,故改為逐個 `PRICE_DRIVER_KEYS` 斷言 `macro_series_needed(key) == ()`,另加驗每行 `EXTERNAL_DATA` 都有序列/來源/免費/知情時間四格且「免費」欄為「是」。判準沒有放寬,只是換了個問法。

· 2026-08-28 09:53 KARST-040-macro:留給 KARST-047 的一件事——本票六個宏觀驅動器裡五個判為孤峰,用的是現行判讀。軸向平原判讀落地之後,這 180 格值得重判一次,尤其曲線斜度那組:它雖是孤峰,但鄰域平均最高格仍有 +3.03%、鄰域 +1.75%,與 KARST-036 那兩個「揀錯格就反轉蝕錢」的價格孤峰不同,現行判讀分不出這個差別。

· 2026-08-28 09:53 KARST-040-macro:實測記錄——`^VIX3M` 的歷史尾段停在 2026-07-17(`^VIX` 到 2026-08-26),主日曆上尾段缺 25 日,期限結構開關在最後 25 個交易日沒有訊號、沿用熱身權重。`^VXV` 已經消失。若 `^VIX3M` 長期不更新,期限結構驅動器要換來源。
