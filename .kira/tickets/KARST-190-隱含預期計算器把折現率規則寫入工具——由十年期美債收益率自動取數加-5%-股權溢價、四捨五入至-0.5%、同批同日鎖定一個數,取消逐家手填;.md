---
id: KARST-190
title: 隱含預期計算器把折現率規則寫入工具——由十年期美債收益率自動取數加 5% 股權溢價、四捨五入至 0.5%、同批同日鎖定一個數,取消逐家手填;敏感度 ±1 個百分點自動輸出
type: task
createdAt: 2026-09-09
risk: low
model: sonnet
fits: KARST-184 流程檢討與 KARST-178 第二節預登記規則(折現率規則);第二份外評報告查出「規則只活在流程層,工具說明仍容許手填」的不一致
dependsOn: []
claimedBy: drate190-sonnet
deliverable: KARST-D04
closed: 2026-09-11
---

## 工作內容

工具在 strategy/tools/implied_expectations.py,說明在 strategy/tools/README.md。現況:每家公司在檔內有一格固定假設含折現率,可逐家手填(SNOW 10%、AXTI 12% 就是這樣來的)。改法:(一)新增取數函數,由 FRED 系列 DGS10(https://fred.stlouisfed.org/graph/fredgraph.csv?id=DGS10)或 yfinance ^TNX 取最近收市的十年期美債收益率,兩個來源都試,記錄來源與日期;(二)折現率 = 收益率 + 5.0 個百分點,四捨五入至最近 0.5%;(三)每次跑一批候選時只取一次,寫入該批的輸出 JSON 頭部(rate、來源、取數日期),所有公司共用;(四)公司層的折現率欄改為只讀,若檔內仍有手填值則印警告並忽略,除非命令列明確傳 --override-discount-rate 並在輸出標明「手動覆寫」;(五)敏感度自動多跑 ±1 個百分點兩組並輸出;(六)README 改寫折現率一節,刪去「新增一家公司要在檔內加折現率」的說法,寫明規則、來源、覆寫方式與代價。改完對 SNOW、AXTI、LULU 三家重跑一次,確認三家共用同一個折現率並把前後差異(基準每股值)列在 README 末的更正紀錄。

## 驗收條件

- [x] 工具由 FRED 或 yfinance 自動取十年期美債收益率並按規則算出折現率,一批共用一個數並記錄來源日期
- [x] 公司層手填折現率預設被忽略並印警告;覆寫要明確旗標且輸出標明
- [x] 敏感度 ±1 個百分點自動輸出;README 折現率一節重寫並附三家重跑前後差異
- [x] 不改計算邏輯其他部分;不改 karst/;含中文檔案只用 Read/Write/Edit;不 commit

## 結果

· 2026-09-09 01:49 折現率規則寫入 `strategy/tools/implied_expectations.py`,`--full` 批次跑不再逐家在 `CASES` 讀 `wacc`。新函數 `fetch_treasury_10y()` / `rule_discount_rate()` / `_round_half_up()`:FRED `DGS10` 與 yfinance `^TNX` 兩個來源都試,取日期較新那筆(實測 FRED 只到 2026-09-03 的 4.77%,`^TNX` 到 2026-09-08 的 4.79%,取後者)。折現率 = 4.79% + 5.0% 股權溢價 = 9.79% → 四捨五入至 10.0%(用 `decimal.ROUND_HALF_UP`,非 Python `round()` 的銀行家捨入,已單元測過 9.25%→9.5%、9.75%→10.0% 兩個邊界都對)。批次共用一個數:`main()` 在 `--full` 分支只取一次,經 `analyse(..., discount_rate=..., manual_override=...)` 傳給每家公司,輸出 JSON 加一個新的 `_meta.discount_rate` 頭部(不動既有 `res[TICKER]` 結構)。公司層手填值變唯讀:`CASES[ticker]` 仍有 `wacc` 鍵時一律印 `[注意]` 警告並忽略(實測 AXTI 印出「已忽略,改用批次規則值」);`--override-discount-rate 0.12` 明確傳了才採用,警告改印「人手覆寫……不採用」,`--full` 分支原本沒印 `fin.notes`(舊漏洞,一併補上,不然警告印不出來)。不傳 `discount_rate` 直接呼叫 `analyse(ticker)`(KARST-184/186 兩支重跑腳本的呼叫方式)完全不受影響——實測 `IE.analyse('AXTI')` 不傳這個參數時 `base.wacc` 仍讀回 `CASES` 的 12%,不印警告,與改動前一致;KARST-188 的 `run_60_full.py` 直接呼叫 `build_financials`/`Assumptions`/`value`/`solve`,完全不碰 `CASES` 或 `analyse()`,本票改動對它零影響。敏感度新增 `rate_sensitivity` 欄位(±1 個百分點絕對值,基準情境),CLI 另段印出,不覆蓋原有 `valA_sens` 的相對 ±10%。README `strategy/tools/README.md` 新增「折現率規則(KARST-190 釘死)」一節,「用法」段刪去「新增一家公司要在檔內加折現率」的舊說法並補 `--override-discount-rate` 範例,限制第 4 條改寫,末尾補「更正紀錄」一節。三家重跑(SNOW、AXTI、LULU,`--full`)前後基準每股值:SNOW 舊手填 10.0%/新規則 10.0%,42.72→42.72(不變);AXTI 舊手填 12.0%/新規則 10.0%,13.64→15.60(+1.96);LULU 舊手填 10.0%/新規則 10.0%,156.77→156.77(不變)。三家共用同一個折現率(10.0%),只有 AXTI 的舊手填值偏離規則值,基準每股值因此改變;SNOW、LULU 的舊手填值本來就與今日規則值巧合相同,數字不變不代表規則沒生效。LULU 原本不在共用工具的 `CASES`(只存在於 KARST-184 的一次性 monkeypatch 腳本),本票把它的固定假設併入 `CASES`;現金取數不用再另外套那份腳本的資產負債表更正,因為 KARST-186 已把同一個修復做進 `instant_series()` 的候選標籤後備清單(實測 `build_financials('LULU')` 現金自動取到 13.90 億)。完整重跑紀錄(含 `_meta.discount_rate`、`rate_sensitivity`、每家 `[注意]` 警告文字)存於 `research/2026-09-methodology/2026-09-09-隱含預期計算器折現率規則重跑.json` 與同名 `.txt`。驗證方式:`python -m py_compile` 通過;跑過簡易模式(無 `--full`,確認舊行為不變)、`--full`(三家一齊跑,確認警告與敏感度都印得出)、`--override-discount-rate 0.12`(確認手動覆寫標籤與計算都對)三種路徑,另用 `IE.analyse('AXTI')`(不傳 `discount_rate`)確認舊呼叫方式輸出與改動前一致——全部命令列真的執行過,不是只讀程式碼推論。

## 留言
