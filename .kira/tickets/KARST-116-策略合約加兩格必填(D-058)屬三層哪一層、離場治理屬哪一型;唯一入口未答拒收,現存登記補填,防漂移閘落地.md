---
id: KARST-116
title: 策略合約加兩格必填(D-058):屬三層哪一層、離場治理屬哪一型;唯一入口未答拒收,現存登記補填,防漂移閘落地
type: task
createdAt: 2026-08-30
risk: medium
model: opus
fits: 一程
approvalRequired: false
dependsOn: []
claimedBy: KARST-116-agent
epic: V1 建置
deliverable: KARST-D02
closed: 2026-08-31
---

## 工作內容

依 D-058(承 D-054/D-056、KARST-115 審視「最急改編制」一項):策略合約(karst/ 的 strategy contract 宣告)加兩格必填——(1) `layer`:此策略屬由上而下三層的哪一層(市況/板塊/個股);(2) `exit_governance`:離場治理分型(延續型注 continuation / 回歸型注 reversion / 規則型 rule-based)。兩格由唯一入口把關:登記或更新策略時未答即拒收,錯誤訊息要講明依據(D-054/D-056)與可選值。現存已登記策略補填:因子混合=個股層/規則型並候 KARST-117 標封存、因子輪動示例照其實際形態填、其餘照現狀誠實填,補填一樣經唯一入口留痕。詞彙表 CONTEXT.md 已有延續型注/回歸型注定義,程式取英文名。測試:拒收路徑與補填路徑各有測試;現有測試全部照過。介面不動(D-057 介面押後)。不碰 .kira/ 其他票;生產庫寫入一律經唯一入口;含中文檔案只用 Read/Write/Edit;不建目錄連結指向 data/;不遞迴刪 repo 外目錄;不 commit。

## 驗收條件

- [x] 新策略登記未填 layer 或 exit_governance 被唯一入口拒收,錯誤訊息指明依據與可選值;測試覆蓋拒收與成功兩路
- [x] 現存全部已登記策略兩格補填齊,經唯一入口留痕,值與現狀相符
- [x] 現有測試全綠;介面零改動

## 結果

· 2026-08-31 00:35 兩格必填落地:策略合約(`karst/executor/contract.py` 的 `StrategyContract`)加
  `layer`(regime 市況 / sector 板塊 / stock 個股,D-054)與 `exit_governance`(continuation 延續型注 /
  reversion 回歸型注 / rule_based 規則型,D-056)。取值正本是庫身 CHECK,`karst/store.py` 給程式一份
  名字(`LAYERS`、`EXIT_GOVERNANCES`),合約與入口轉引,不另寫第二份。

· 2026-08-31 00:35 唯一入口把關,未答拒收:`Gateway.register_strategy` 兩格必填,經
  `DefinitionStore._check_layer` / `._check_exit_governance` 當場拒收,**庫內一個字都寫不入**。
  訊息樣本(缺層別):「策略「X」缺層別(layer):平台重心是由上而下三層,任何策略按此次序收窄,
  不准跳層由個股起步(D-054);regime(市況層)、sector(板塊層)、stock(個股層) 揀一個。無預設值
  ——講不出自己站在哪一層的策略,無從判它有沒有跳層」;缺離場治理那句引 D-056 第 2 條並列三個可選值。

· 2026-08-31 00:35 遷移手法照最近一次同類加欄(KARST-094 對齊標記):schema 升第 17 版,加**旁表**
  `strategy_governance`(追加式、不可改不可刪、入治理清單 `GOVERNED_TABLES` 歸「定義」類、逐列簽章)。
  不做 `strategy` 多兩欄的兩個理由都是硬的:該表有 `trg_strategy_no_update` 擋 UPDATE,現存三條登記
  永遠補填不到;而且多兩欄會改動 content_digest,既有簽章一次過作廢。

· 2026-08-31 00:35 補填路徑:新命令 `karst strategy declare-governance --name … --layer … --exit-governance …
  --basis "…"`,同一道門、同一種簽章;依據一句不准留空。改宣告 = 加一筆新宣告,舊宣告一字不變。

· 2026-08-31 00:35 防漂移閘:策略已在庫時,執行台 `check_governance` 核對合約宣告與庫內宣告——
  未宣告即照合約補一筆(留簽章),對不上即當場拒收(哪一邊才對不是執行台判得出的事,靜靜覆蓋等於
  把當日那個判斷抹走)。趨勢波段自己那條登記路徑一樣走這道閘。

· 2026-08-31 00:35 生產庫三條補填齊(經唯一入口,寫入者 KARST-116-backfill):
  因子混合(ETF 版)= 個股層 / 規則型(依票裁定);因子輪動(ETF 版)= 個股層 / 規則型;
  趨勢波段 = 個股層 / 延續型注。
  **判法**:因子輪動與因子混合蓋住同一四格因子敞口、同一個收窄步驟,分別只在每期比重由驅動器算——
  同一個宇宙的兩條策略若果站在不同層,三層次序就不再指得出任何東西,所以跟混合同層;它無價格止蝕、
  無賠率門檻、無論點失效條件,離場只是每期問一次驅動器再重算比重(權重歸零即清倉、加總少於一即餘下
  持現金),屬規則型。趨勢波段:突破 N 日新高入場、押強者屬個股層;止蝕擺位 + 賠率門檻 + 不溝貨三件
  正是詞彙表「延續型注」的逐字對應。
  **一個未完全踏實的判斷**:四隻因子 ETF 放進三層並不完全貼服(既不是市況、亦不是 GICS 板塊、亦不是
  個股)。本次照票的裁定把因子混合放在個股層,輪動為求同一宇宙同一層跟它一致;日後若果把因子 ETF
  正式歸類為另一種籃子,兩條一齊加一筆新宣告即可,舊宣告一字不變。

· 2026-08-31 00:35 核對(`experiments/2026-08-30-strategy-governance/verify_116.py`,全程唯讀):
  8,318 次運行逐條逐格與動庫前備份比,**零格出入**(兩格走旁表、不進參數集內容,運行編號一位不變);
  51,845 列既有簽章零消失零改動,新增剛好 3 列且全部屬 `strategy_governance`;
  `karst verify` 三類全部清白(定義 50,829 列、因子批次 997 列、快照 22 列)。

· 2026-08-31 00:35 測試:新增 `tests/test_strategy_governance.py` 十條(拒收三條、成功與簽章兩條、
  補填與追加三條、依據不可空一條、防漂移閘一條)全綠;全倉 `pytest` **385 passed, 6 skipped**
  (6 條 skipped 與改動前同一批)。介面 `karst/web/` 零改動(git diff 空)。

· 2026-08-31 00:35 詞彙表 `CONTEXT.md` 加三條:規則型(rule-based exit)、離場治理(exit governance)、
  策略治理宣告(strategy governance declaration)。「規則型」是本票新鑄的第三型,D-056 原本只有兩型。

## 留言
