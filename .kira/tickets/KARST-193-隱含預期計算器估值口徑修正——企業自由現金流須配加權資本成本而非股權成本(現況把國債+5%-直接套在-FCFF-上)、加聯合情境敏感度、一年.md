---
id: KARST-193
title: 隱含預期計算器估值口徑修正——企業自由現金流須配加權資本成本而非股權成本(現況把國債+5% 直接套在 FCFF 上)、加聯合情境敏感度、一年回報表改為四情境並把「倍數不變」放第一
type: task
createdAt: 2026-09-09
risk: low
model: opus
fits: 第二輪外部評論「優先修改四處」第一、二項;主 agent 已核實工具第 493 至 497 行以 fcff 折現得 EV 再減淨負債,而 KARST-190 的折現率規則是股權成本口徑,兩者不配對
dependsOn: []
claimedBy: calcpair193-opus
deliverable: KARST-D04
closed: 2026-09-11
---

## 工作內容

對象 strategy/tools/implied_expectations.py 與 README.md(KARST-190 剛改過折現率規則,保留其批次共用與唯讀邏輯,不倒退)。(一)口徑配對:保留 FCFF→EV→減淨負債得股權的架構,但折現率改為加權資本成本:股權成本 = 十年期美債 + 5%(沿用 190 規則,四捨五入前的原值),債務成本 = 十年期美債 + 信用差價(第一版用固定 2.0 個百分點,README 寫明是示例待對齊)× (1 − 稅率),權重用市值與有息負債帳面值;淨現金公司(有息負債 ≤ 現金)WACC 與股權成本相同,印一句說明;四捨入至 0.5% 在 WACC 層做。輸出 _meta 同時記股權成本、債務成本、權重、WACC。對 SNOW、AXTI、LULU 及 KARST-184 其餘四家(TDC、ON、ENPH、ARM)重跑,列前後基準每股值差異;預期淨現金公司差異為零、有債公司價值上升,量出來。(二)聯合情境:除逐項 ±10% 與折現率 ±1 個百分點外,加三組聯合情境(悲觀:增長−10 個百分點且目標利潤率−5 個百分點且折現率+1;中性:全部基準;樂觀:反向),輸出每股值。(三)一年回報表改為四情境固定次序:1 經營按基準路徑、倍數不變;2 經營改善(收入或利潤率按樂觀路徑)、倍數收縮至近一年 25 分位;3 具體反證出現、倍數回到近一年中位(要在 notes 寫「重估理由」欄,由使用者填,空白即印警告);4 論點失效(收入按悲觀路徑、倍數近一年 25 分位)。原有情境保留為附錄不刪。README 寫明四情境定義與「倍數變化必須接到盈利能力/增長/風險/資本配置的具體變化」這條規則。(四)README 末更正紀錄。向後相容:輸出 JSON 只加欄不刪欄;舊呼叫方式仍可跑。

## 驗收條件

- [x] 折現率改為 WACC 配 FCFF,_meta 記齊股權成本/債務成本/權重;七家重跑前後差異表落檔,淨現金公司差異為零得到驗證
- [x] 三組聯合情境敏感度輸出;一年回報表四情境固定次序,「倍數不變」第一,重估理由欄空白印警告
- [x] README 改寫並附更正紀錄;JSON 只加欄不刪欄;舊呼叫方式實測仍可跑
- [x] 不改 karst/;含中文檔案只用 Read/Write/Edit;Python 一律 PYTHONUTF8=1;不 commit

## 結果

· 2026-09-09 03:10 **(一)口徑配對。** `strategy/tools/implied_expectations.py` 新增 `CREDIT_SPREAD = 0.02` 與 `cost_of_capital()`:股權成本 = 十年期美債 + 5.0 個百分點(取四捨五入前原值),稅前債務成本 = 美債 + 信用差價,稅後乘 (1 − 稅率),權重用市值(現價 × 稀釋後股數)對有息負債帳面值(`fin.debt`,不含租賃),四捨五入至 0.5% 改在 WACC 層做。淨現金判定用「有息負債 ≤ 現金」(對齊 D-162 的 N2 口徑),命中即 WACC = 股權成本並印一句說明。`rule_discount_rate()` 加 `credit_spread` 參數並多記 `cost_of_equity_raw` / `cost_of_debt_pretax` / `basis`;`analyse()` 加 `rate_inputs=` 與 `rerating_reasons=` 兩個有預設值的參數,折現率四條路由上而下先中先用(手動覆寫 → `rate_inputs` 算 WACC → `discount_rate` 全批共用 → `CASES` 舊手填),前後三條全部是既有行為、一句未改。CLI 加 `--credit-spread`、`--net-cash-include-investments`。`_meta` 加 `cost_of_capital[代號]`,記股權成本、稅前/稅後債務成本、市值、有息負債、兩個權重、未捨與已捨 WACC、是否淨現金。`CASES` 併入 TDC / ON / ENPH / ARM(假設原封搬自 KARST-184 的 `run_three_numbers.py`,一格未改)。七家重跑(十年期美債 4.802%,yfinance `^TNX` 2026-09-08;FRED `DGS10` 同日只到 09-03 的 4.77%):**淨現金四家 AXTI / LULU / TDC / ARM 差異全部正好 0.00**(WACC 定義上等於股權成本 10.0%),有債三家上升——SNOW 42.72 → 47.95(+12.2%,WACC 9.5%)、ON 33.17 → 39.08(+17.8%,WACC 9.0%)、ENPH 24.50 → 26.19(+6.9%,WACC 9.5%)。差異另外拆成「槓桿本身」與「四捨五入跨界」兩欄:SNOW +5.22 之中只有 +0.91 來自槓桿、+4.31 來自取整(債務權重只有 1.92%,未捨 WACC 9.715% 剛好跌穿 9.75% 那條界);ON 是 +3.58 / +2.33;ENPH 是 +1.65 / +0.04。**(二)聯合情境。** 新增 `valA_joint`(增長 ∓10 個百分點、目標利潤率 ∓5 個百分點、折現率 ±1 個百分點三項一齊撥),原有(甲)悲觀/基準/樂觀三格不動、並存。LULU 三組:64.63 / 156.77 / 345.28。**(三)一年回報表四情境。** 新增 `one_year_four_scenarios()` 與 `FOUR_SCENARIOS` 固定次序(1 基準路徑+倍數不變、2 樂觀路徑+近一年 25 分位、3 基準路徑+近一年中位、4 悲觀路徑+近一年 25 分位),退出倍數改用近一年分位(不足才退回四年並說明),收入格由 `g1_cases` 按增速排序自動配悲觀/基準/樂觀。「重估理由」欄由使用者填(`RERATING_REASONS` 或 `analyse(rerating_reasons=...)`),情境 2/3/4 空白逐行印警告但不阻止跑,情境 1 自動填「不適用」。原有 3×4 網格保留為附錄,一行沒刪。**(四)** README 改寫(「資本成本規則」一節重寫、新增「聯合情境」與「一年回報表四情境」兩節、限制加第 10–13 條、末補更正紀錄連前後差異表)。**向後相容實測(四項全過):** 舊呼叫 `analyse(t)` 不傳折現率仍讀 `CASES` 手填值(SNOW 42.72 @10%、AXTI 13.64 @12%,與舊值一致);`analyse(t, discount_rate=0.10)`(KARST-190 路徑)重現該票更正紀錄三個數(42.72 / 15.60 / 156.77);`--override-discount-rate` 手動覆寫仍全批共用、不算 WACC;KARST-188 式 `build_financials` + `Assumptions(...)` + `value()` + `solve()` 照跑,`Assumptions` 十二個欄位次序名稱一格未動,`value()` / `solve()` 簽名未改。輸出 JSON 逐鍵比對 KARST-190 那份:**舊欄位一個不缺**(唯一「缺」的是 `mult_cases` 那個內嵌當日倍數的標籤鍵 23.3x → 23.2x,不是欄位),新增 `wacc_detail`、`valA_joint`、`oneyear_four`、`rerating_warnings`、`_meta.cost_of_capital`。另跑過非 `--full` 簡易模式與雙票 `--full --json`,全部真的執行過。落檔:`research/2026-09-methodology/2026-09-09-隱含預期計算器口徑修正重跑.md`(報告)、同名 `.json` / `.txt`(完整重跑)、`2026-09-09-KARST193-前後差異.csv`(逐家明細含拆解欄)、`rerun_193_cost_of_capital.py`(重跑腳本)。`CONTEXT.md` 詞彙表加「重估理由 / rerating reason」與「聯合情境 / joint scenario」兩條。不碰 `karst/`、`library/`,不 commit。

## 留言

· 2026-09-09 03:10 raised:**「淨現金」用哪個口徑,要人裁。** 現行按票的字面用「有息負債 ≤ 現金」(對齊 D-162 的 N2)。SNOW(現金 20.85 億 < 有息負債 22.82 億,但現金加證券 43.88 億)與 ENPH(4.98 億 < 5.73 億,加證券 9.31 億)兩家卡在界上:一旦把短期及長期投資也當現金,兩家就翻成淨現金公司、每股值退回舊值(SNOW 47.95 → 42.72、ENPH 26.19 → 24.50)。工具已備 `--net-cash-include-investments` 兩邊都跑得出,預設維持票的字面口徑,但同一家公司同一日兩個答案,用哪個要有人決定。

· 2026-09-09 03:10 raised:**四捨五入到 0.5% 這一步製造假訊號。** SNOW 債務權重只有 1.92%,槓桿本身只值每股 +0.91,但未捨 WACC 9.715% 跌穿 9.75% 界被推到 9.5%,取整額外送 +4.31——七成以上的價值變動來自取整規則。票明文要求「四捨五入在 WACC 層做」,已照做,但要不要保留這一步(或者改成只在顯示時取整、計算用原值)請裁。

· 2026-09-09 03:10 raised:**經營租賃負債不入 WACC 權重,LULU 因此被判為淨現金公司。** 照票的「權重用市值與有息負債帳面值」實作,租賃負債照舊留在淨負債裡。但 LULU 有 21.41 億租賃負債、零有息負債,租賃債務化之後它其實不是無槓桿的公司。要改成把租賃計入權重,是另一個口徑決定。

· 2026-09-09 03:10 **四情境的名字在一種情況下會與方向相反,已加自動警告。** 現價市銷率低於近一年 25 分位時(LULU 2026-09-08 現價 1.0x 對 25 分位 1.2x),情境 2 與 4 的「倍數收縮至 25 分位」實際上是倍數上調,情境 4「論點失效」錄得 +12.5%。工具偵測到即印警告叫人反過來讀,七家只有 LULU 觸發。這是固定次序表本身的邊界,不是計算錯誤,先記在這裡。
