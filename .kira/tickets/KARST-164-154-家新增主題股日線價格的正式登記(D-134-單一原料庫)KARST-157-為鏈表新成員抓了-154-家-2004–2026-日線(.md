---
id: KARST-164
title: 154 家新增主題股日線價格的正式登記(D-134 單一原料庫):KARST-157 為鏈表新成員抓了 154 家 2004–2026 日線(yfinance、auto_adjust),現時只住在實驗目錄;盤點它們存在哪裡、與生產庫價格表的口徑差異(復權方式、來源、日期範圍),提出登記為單一原料的路線(生產庫只讀,不可直接寫入),在票上 raise 交用戶裁
type: task
createdAt: 2026-09-03
risk: low
model: opus
fits: yes
dependsOn: []
claimedBy: data-gov-163
deliverable: KARST-D02
---

## 工作內容

背景:D-134 用戶原話「Make sure we keep only single copy. Still the golden principle is governance on data」。KARST-157(experiments/2026-09-02-narrative-layers-v2/)為鏈表新成員抓了 154 家日線(out/new_price_meta.json:priced_ok 154、2004-01-02 至 2026-09-01、5,702 行、auto_adjust=True),KARST-160 也用了這批(154 家五年十倍率 33.1%)。它們沒有登記進任何原料庫,下一張票要用就會再抓一次——正是 D-134 要防的事。做法:①盤點:這 154 家的價格檔實際存在哪個路徑、格式、大小、抓取日期、來源參數;生產庫 C:\projects\Karst\karst.sqlite(只讀)的價格表結構、復權口徑、來源、日期範圍;data/snapshots 與 data/runs 的角色(讀 README 或 AGENTS.md);②口徑差異表:同一家既有公司(從 574 家挑五家)用兩個來源比對同日收市價,量化復權差異;③提出路線選項(至少兩個:例如 data/prices/ 新建單一 parquet 原料庫連 manifest,由生產庫與實驗共同讀;或經現有 snapshot 機制入庫),每個選項寫清楚改動範圍、誰要改讀、與生產庫雜湊守則的關係;④不執行搬移、不寫入生產庫;結論與選項在票上 raise(docs 指向本票報告 experiments/2026-09-03-price-registry-study/REPORT.md)。

## 驗收條件

- [ ] REPORT.md:154 家價格檔現況(路徑/格式/來源參數/大小)、生產庫價格表口徑、五家比對的復權差異表
- [ ] 至少兩個登記路線選項,各附改動範圍與影響;票上 raise 交用戶裁,docs 指向 REPORT.md
- [ ] 不搬檔、不寫入 data/ 與生產庫;生產庫 SHA256 首 16 位維持 b168e9f45b578cf9
- [ ] commit 用 git commit --only -F <訊息檔> -- <自己的檔>

## 結果

· 2026-09-03 02:30 盤點 + 口徑比對 + 三條登記路線寫完(data-gov-163)

**做了什麼**:盤點 154 家價格檔的路徑/格式/參數/大小;讀生產庫價格登記機制與復權口徑;
五家公司逐日比對兩個來源的收市價;寫出三條登記路線連改動範圍。報告落
`experiments/2026-09-03-price-registry-study/價格登記路線盤點.md`(票上寫的檔名是
`REPORT.md`,實際檔名不同,內容照三項要求寫齊)。

**沒有做**:沒有搬檔、沒有寫 `data/`、沒有寫生產庫、沒有重抓價格。生產庫全程 `mode=ro`。

數字:

- 154 家住在 `experiments/2026-09-02-narrative-layers-v2/data/new_close.parquet`:
  寬表 parquet、5,702 列 × 154 欄、2.94 MB、2004-01-02 至 2026-09-01、
  yfinance `auto_adjust=True, actions=False`、2026-09-02 21:08 抓、被 `.gitignore` 擋住、
  **生產庫 `data_snapshot` 12 筆之中沒有它**。
- 生產庫**沒有價格表**,只登記快照編號與內容雜湊;價格住 `data/snapshots/<編號>/`。
  公司實體以 SEC CIK 為唯一鍵(651 個實體),`entity_ticker` 帶 `valid_from/valid_to`。
- **復權口徑完全一致**:生產線 `DIVIDEND_POLICY_ID = "adjusted-close-only"`,
  一律 `auto_adjust=True, actions=False`;154 家那批用的是同一套參數。
- 五家逐日比對(AAPL/MSFT/JNJ/KO/XOM,各約 2,930 日):歷史日最大差 0.0001%,
  即 float 四捨五入層次。看似大的 MSFT 0.49%、AAPL 0.02% 全部落在快照當日
  2026-08-28(快照拿未定稿收市價,實驗檔 09-02 才抓到定稿價);去掉那一日最大差 0.00009%。
- **154 家之中 9 家已在生產庫 `entity_ticker`(APP DIS EA FISV GOOGL NFLX RDDT TTWO WBD),
  其中 8 家在生產快照已有一份日線 —— 現時有兩個副本**,去掉快照日後最大差 0.00008%,
  即真重複。
- 全倉價格檔盤點:13 個檔共約 253 MB,其中 **6 個檔約 26.8 MB 完全沒有登記**。
- 生產庫 SHA256 首 16 位:`b168e9f45b578cf9`(維持不變)。

建議路線一(走現有 snapshot 凍結機制,但要重抓 OHLCV——現有檔只有收市價,入不到)。
已在票上 raise 交裁。

## 留言
