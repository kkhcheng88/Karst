---
id: KARST-221
title: 共用層 basket_core.py 市值三處改碼(未還原收市價、封面股數按拆股重列、缺股數標查不到)並只重出 KARST-199 市銷率與市帳率兩條事前特徵核「估值平者勝」結論是否成立;其餘舊表加一行標註不重出
type: task
createdAt: 2026-09-12
risk: low
model: sonnet
fits: KARST-219 舉手裁決(改碼 + 只重出 199 兩條);修法與驗證個案已在 research/2026-09-methodology/2026-09-12-暴露差異模組v1/fix_mcap.py 與 修復——mcap_usd.md;199 的「分得開的是估值(市銷率平者勝)」是被引到結論層的唯一市值衍生數
dependsOn: []
claimedBy: mcapcode-221b-ds
epic: 方法論期(D-166)
deliverable: KARST-D04
closed: 2026-09-12
---

## 工作內容

一、改碼:找出 basket_core.py(Grep research/ 與 strategy/tools/),按 fix_mcap.py 的修法改三處——價格改用未除息還原的收市價(data/prices/daily/ 的 close 而非 adj_close)、封面股數按拆股表重列(拆股表若不在 data/ 則用 fix_mcap.py 已用的來源並寫明)、缺股數者 mcap_usd 標 NaN 並加 mcap_status 欄;改前備份;以 218/219 驗證個案(NVDA 2024-08-01、AT&T 2013-05-21 等)重跑核對誤差為零。二、重出 199:用修後市值重算 2026-09-10-①行業殺錯事件籃子/ 的市銷率(EV/S 或 P/S,照原口徑)與市帳率兩條事前特徵對十二個月結果的分組(照原腳本 feature 分析的三分位做法),另存 *_mcap修正.csv 與一頁對照(修前/修後分組中位、結論有沒有變);原檔不改。三、標註:在 204 checklist_test、210、212–214 三批、218 的總覽檔尾各加一行「市值欄(mcap_usd)含 2026-09-12 前舊值,經 KARST-219 查證不影響本檔結論;修正值見 2026-09-12-暴露差異模組v1/mcap修正/」——用 Edit 只加行不改原文。含中文檔案只用 Read/Write/Edit;Python 一律 PYTHONUTF8=1;不改 karst/ library/ strategy/tools/;不 commit。

## 驗收條件

- [x] basket_core.py 三處改碼,備份在,驗證個案誤差為零,重跑腳本可用
- [x] 199 兩條特徵修前修後對照落檔,結論有沒有變寫明
- [x] 受影響檔尾標註一行各加齊;含中文檔案只用 Read/Write/Edit;Python 一律 PYTHONUTF8=1;不 commit

## 結果

**一句:三處改碼已齊,生產者碼與 KARST-219 參考實作逐行一致、單元個案零誤差;但 199 的「估值平者勝」在市值修正後不再成立,已舉手等人裁(見票頭 `raised:`)。**

一、三處改碼（`research/2026-09-methodology/2026-09-10-①行業殺錯事件籃子/basket_core.py`）：(1) 市值改用未除息還原收市價 `close`（新 `raw_close_asof` 取 ≤ t 的那個價），不再用 `adj_close`；(2) 封面頁股數乘回該筆申報日之後的拆股比率（新 `split_factor_after`＋`load_splits`，拆股表用 `mcap修正/splits_used.csv`——data/ 內沒有公司行動表，價格庫 `close` 的調整來源正是這張表）；(3) 取不到股數即 `mcap_usd` 留 NaN 並加 `mcap_status` 欄（有／無股數／無價／無面板）。備份在 `mcap修正/basket_core.py.2026-09-12.karst221.bak`（改前原版另可用 `git show HEAD:` 還原；`~/.claude/backups/` 被權限擋）。

二、驗證（`mcap修正/驗證——生產者碼對參考值.txt`，腳本 `verify_mcap_patch.py`）：單元個案（NVDA 2024-08-01、T 2013-05-21 等 17 宗）逐宗誤差 **0 元**；199 全 4,374 行逐行對 `fix_mcap` 修正值，最大相對誤差 2.22e-16（浮點最後一位，乘的次序不同），468 行屬此類；另有 173 行參考有值而生產者留 NaN，是口徑不同（`fix_mcap` 在面板過期時用外補股數，生產者照票面第二條標查不到），不是算錯。註：`load_prices`＋`build_wide` 全量要 ~5GB（實測單份 330MB × 16），前一個工人因此被中止；本腳本改用 arrow 逐份過濾，只載入用得到的 entity。

三、199 重出（`out/*_mcap修正.csv`、`對照——市值修正後兩條特徵.md`，原檔未改）：**結論變了**。同 SIC 籃子市銷率事件內 AUC 0.404→0.487、市帳率 0.418→0.518，兩條都由「分得開」退回「分不開」；市銷率三分位（衝擊低點錨）由 低 −1.5%／中 −4.1%／貴 −17.3% 變成 −6.0%／−5.1%／−8.8%（單調消失），市帳率更由貴者最差變成貴者最好。樣本同一批（市銷率有值 2,842 行、市帳率 3,035 行，修前修後一個不多一個不少），修前欄與已發表的 199 完全一致。附錄一併重砌「三條問題」表：方向仍在，零條與三條的中位差 18.2pp→13.2pp。

四、標註：六檔各加一行（204 `checklist_test/總覽——檢查清單歷史考試.md`、210 `2026-09-11-①並排歷史考試/總覽——①並排歷史考試.md`、212–214 `2026-09-11-①v3全量回測/批1、批2、批3/總覽——批N.md`、218 `2026-09-12-暴露差異模組v1/總覽——樣本外新事件.md`），只加行不改原文。

## 留言

### agent:mcapcode-221b-ds · 2026-09-12 02:55
**問題**：199 的「估值平者勝」在市值修正後不再成立——199 應否改讀為「現金流與利潤率分得開、估值分不開」,並據此更新地圖 199 格與①模組對外說法?

**前文後理**：KARST-219 查出 199 的市值欄用除息還原價乘未還原股數,高息股被低估。KARST-221 修好生產者碼後只重出市銷率、市帳率兩條:同 SIC 籃子兩條的事件內 AUC 都由 0.40／0.42(分得開)退回 0.49／0.52(分不開);市銷率三分位的貴者中位超額由 −17.3% 收窄到 −8.8%,單調消失;市帳率更由「貴者最差」變成貴者最好。樣本與修前完全同一批(市銷率有值 2,842 行、市帳率 3,035 行,一個不多一個不少),修前數值與已發表的 199 完全一致;經營現金流利潤率、淨利率兩條不經市值,不受影響。199「三條問題」表也重量了:方向仍在,零條與三條的中位差由 18.2pp 收窄到 13.2pp。

**建議**：建議照新數改讀,並在改寫前給外部評審知道——這是 199 唯一撐住「估值」那一半的證據,而它本來就只靠一個有瑕疵的欄;其餘結論(現金流利潤率、淨利率、整籃不是優勢)不要動。地圖 199 格那一句「分得開的是現金流利潤率與估值」應改為「分得開的是現金流利潤率與淨利率」。

**選項**：
- 甲（建議）：照新數改讀 199,並更新地圖 199 格與①模組對外說法 —— 樣本同一批、修前數值可對回原文,證據乾淨;錯的那一半不應留在結論層
- 乙：先補做顯著性檢定與外部股數補值(修後仍有 1,100 行取不到市值),再定改讀 —— 想先排除「只是樣本薄」這個可能;代價是 199 的錯要帶著走多一輪
- 丙：199 原文不動,只把對照附錄存檔,等 ① 收官一併改 —— 避免逐宗改結論;風險是中間任何引用仍會用到已被推翻的那一句

**要睇邊份稿**：
- research/2026-09-methodology/2026-09-10-①行業殺錯事件籃子/對照——市值修正後兩條特徵.md

### agent:mcapcode-221b-ds · 2026-09-12 02:55
**交接（續做,上一個工人被系統因記憶體不足中止;本輪由 mcapcode-221b-ds 接手）**

上一位已改好的三處碼與 `verify_mcap_patch.py` 沒有重寫,只核齊三處後補了兩樣:該腳本原本用 `load_prices()`＋`build_wide()` 建日曆,那一步要 ~5GB（實測單份 parquet 330MB × 16 份）,已改為 arrow 逐份過濾、只載入用得到的 2,413 家;另把「三層誤差全為零」那個一刀切判詞拆開講（A 層 2.22e-16 屬浮點最後一位、173 行是口徑不同）。

結果:三處齊、單元個案零誤差、199 兩條重出（結論變了,已舉手）、六檔標註加齊。票留開,等收件箱那條舉手有人答。

### agent:main-fable · 2026-09-12 02:57
**裁決**：取甲:199 改讀——事前分得開贏輸的只剩經營現金流利潤率(與營收穩定,若其 AUC 不受市值影響);市銷率與市帳率兩條由「分得開」退回「分不開」(事件內 AUC 0.404→0.487、0.418→0.518),「估值平者勝」一句作廢。199 總覽原文不動,以工人已加的檔尾標註加本票對照檔為更正紀錄;地圖①B 行、①對齊紀錄、清單 v3 前置否決之中「市場為它的銷售額付了多貴」一問的依據同步標「市值修正後不成立,降為紀錄欄」。不補顯著性檢定與外部股數(1,100 行取不到市值照標查不到)——①線已不再投入。

**出處**：主 agent(Fable)依 D-072 裁
