---
id: KARST-256
title: S4 日更規模化:增量 K 線、已存技術面快照、有界並行與逐來源速率上限、背景分段執行、機械分流
type: task
createdAt: 2026-09-23
risk: high
model: opus
fits: 一程(大):daily 執行器與 prices 增量;若超出則按增量取價／並行執行器／分流拆票
dependsOn: [KARST-253, KARST-254, KARST-255]
claimedBy: Opus-S4
epic: 根基重整
deliverable: KARST-D12
closed: 2026-09-23
---

## 工作內容

依執行計劃 §二目標及 §三 4。(1) 已有足夠歷史的證券只取最後完整 K 線之後的新 K 線,經 S2 序列入口接駁;(2) 技術面(均線、ATR、結構)由已存序列快照加新 K 線更新,不每日由全部證據重建;(3) 成員以有界並行執行,每個外部來源有速率上限、退避重試與逾時;(4) MCP 入口改為開始／查進度／續跑,每次呼叫 60 秒內返回,長批次在背景分段進行並逐隻 checkpoint;(5) 增量建置的機械層(用戶 2026-09-23 更正,取代「平靜只推進價格」):每股每日產生「日更快照」——新 K 線、技術指標與結構、距計劃各價位、現價對公允價值空間、R&R、入場／失效／目標觸發狀態,計劃數字每日更新;並按 updates.py 路由表標出需要 agent 重評的層(價格事件 → L5／L6;新聞／研報 → L2／L3／L4／L6),交 agent 隊列。事件門檻按執行計劃 §二「增量建置」。LLM 重評本身與保存時的範圍控制在 KARST-262。覆蓋不完整者不推進新聞窗口。以 S1 基準命令量度 N=200 假成員。程式只住 karst/。

## 驗收條件

- [x] S1 基準 N=200、每來源注入真實量級延遲時,價格＋技術面增量 ≤ 5 分鐘、連新聞 ≤ 10 分鐘(假客戶端量度)
- [x] 已有足夠歷史的證券零次全量重取,補取另計
- [x] 任何一次 MCP 呼叫 60 秒內返回;中斷後可續跑且不重做已完成成員
- [x] 一隻失敗不阻其他;失敗可單獨補跑
- [x] 日更快照每股每日產生,含觸發狀態與 R&R;需重評的層按路由表標出,有定向測試(純價格日、價格事件日、新聞日、覆蓋不完整四類);不完整者新聞窗口不推進
- [x] 技術面快照更新結果與全量重建逐值相同(測試驗證)

## 結果

· 2026-09-23 04:05 六項驗收達成,未拆票;回執 `cards/runs/daily-scale-2026-09-23/after-256.json`
- 四個瓶頸:(1) 成員以 8 條執行緒有界並行,`karst/fetch/limits.py` 按來源設速率、同時連線數、逾時、退避重試,逾時／限流經來源 port 回報為 `timeout`／`rate_limited`;(2) `CompanyBundle.register_many` 一次讀寫每家公司的 manifest 與 observations;(3) 全文索引查重改用有索引的普通表 `evidence_text_ids`(一次性回填);(4) checkpoint 改為表頭檔加逐成員追加的 `.members.jsonl`,MCP 只回摘要與 run_id。另有增量 K 線:已存序列由最後完整 K 線起取新 K 線,口徑或身份不同即全量重建。
- 預設上限:Longbridge 每秒 10 次、同時 5、逾時 20 秒、重試 2;Yahoo／Google RSS 各每秒 2 次、同時 4、逾時 12 秒、重試 2;EDGAR 每秒 8 次、同時 4、逾時 60 秒、重試 1。
- MCP:`refresh_daily_scope` 開背景分段批次,`get_daily_runs(run_id, wait_seconds)` 查進度,`resume_daily_scope(run_id)` 續跑;每次呼叫最多等 40 秒。容器重啟後舊批次讀作 interrupted,按 run_id 續跑只做未完成成員。單股 `refresh_daily` 保留原行為。步驟已寫入 daily-operations.md。
- 日更快照(`karst/daily_snapshot.py`,經 `CompanyBundle.save_daily_snapshot` 原子保存):新 K 線、均線／ATR／量、支阻及日週結構、距計劃各價位、現價對公允價值範圍的位置、收市價的 R&R、入場／失效／目標觸發狀態;不改研究版本、不寫查核、不發布。門檻集中在該檔:跳空 2 倍 ATR、R&R 2:1、結構只計 BOS／CHoCH、隊列保留最近 20 項理由。價格事件排 L5／L6,新聞排 L2／L3／L4／L6,覆蓋不完整不推進新聞窗口;雷達成員只有技術面。
- N=200 假客戶端量度:價格＋技術面 102.9 秒(1.7 分)、連新聞 166.4 秒(2.8 分);基線推算為約 10 分與約 32 分。零延遲 CPU 102.1 秒對基線 222.7 秒;checkpoint 序列化由 156 MB 降至 1.3 MB。現時時間由 RSS 每秒 2 次的上限決定。雲端實跑屬 KARST-261。
- 全套測試 469 通過、1 略過;唯一失敗是既有的 test_reader 符號連結權限(WinError 1314)。

## 留言

### agent:Opus主腦 · 2026-09-23 10:40

改寫第 (5) 項及對應驗收:用戶否決「平靜記錄不改研究判斷」,改為增量建置(原話見執行計劃 §二)。範圍控制另開 KARST-262。已放回未認領。

### agent:Opus-S4 · 2026-09-23 04:05

六項驗收全部達成,未拆票。部署前要注意:(1) 雲端第一次日更會由已登記紀錄為每股重建一次序列,之後才走增量;(2) 舊客戶端若期待 `refresh_daily_scope` 直接回 `results`,要改用 `get_daily_runs(subject=...)`;舊格式的整份回執仍可讀;(3) 同一批次共用一個 Longbridge SDK client,多執行緒下是否安全要在雲端實跑(KARST-261)確認;(4) SQLite 多執行緒寫入沿用預設 5 秒 busy timeout;(5) 版本升為 0.2.14,可能與 KARST-259 撞版本號,合併時要協調。
