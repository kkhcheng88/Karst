---
id: KARST-094
title: 參數集對齊標記落庫(D-038):以經唯一入口簽章的旁表記「已對齊／示例」與對齊日期,不進運行編號雜湊、不動既有簽章;畫面與 verify 讀得到
type: task
createdAt: 2026-08-30
risk: medium
model: opus
fits: 一程
approvalRequired: false
dependsOn: [KARST-090]
claimedBy: agent-094b
epic: V1 建置
deliverable: KARST-D02
closed: 2026-08-30
---

## 工作內容

源自 KARST-090 舉手:參數集的「已對齊／示例」標記若寫入參數集內容,13 條正式運行全部改號或既有簽章作廢;主腦裁決改為旁表。完成後:(1) 庫身升一版,新增參數集對齊標記表(參數集編號、標記值:已對齊／示例、對齊日期、對齊依據一句、寫入者簽章),追加式,不可刪;(2) 唯一入口新增登記/改標記命令,經簽章寫入,入治理清單,karst verify 核得到;(3) 執行台登記參數集時的強制申報改為寫入此表(現有 13 條正式運行所用參數集一律補記「示例」,留痕);(4) 定義庫提供按參數集讀標記的介面,策略頁批次門面與參數區能顯示「示例」或「已對齊(日期)」;(5) 運行編號與既有簽章一個不變(測試:13 條正式運行指紋逐位相同,verify 三類清白)。動庫前備份到 C:\Users\Kaho\.claude\backups\karst.sqlite.2026-08-30-094.bak。不加參數預設值;只跑所涉測試檔;不跑 tests/test_web*.py(打生產庫,KARST-093 處理中);含中文檔案只用 Read/Write/Edit;不建目錄連結指向 data/;不碰 karst/web/static/ 與 prototype/;不 commit。

## 驗收條件

- [x] 對齊標記旁表落庫,經唯一入口簽章寫入,入治理清單,verify 清白;13 條正式運行指紋逐位不變
- [x] 執行台登記時寫入標記;現有參數集全部補記「示例」並留痕;定義庫可按參數集讀回
- [x] 只跑所涉測試檔;備份已做

## 結果

· 2026-08-30 14:34 對齊標記已由記憶體搬入庫身。新表 `param_set_alignment`(參數集編號、申報序號、標記值、對齊日期、依據一句、落庫時間)追加式、不可改不可刪,兩個 trigger 守住;入治理清單並歸「定義」類,逐列有唯一入口簽章。唯一入口新增 `karst params mark-alignment` 命令;執行台 `register_setup` 登記參數集時把 D-038 那個申報同步寫入此表(重覆申報同一件事回上一筆,不會白加一列)。定義庫提供單查、批量查與歷次查三個介面,取數層 `RunReader` 逐次運行帶出 `paramSetAlignment`,`params show` 分得出「未申報／示例／已對齊(日期)」三句話。生產庫那 13 個正式運行所用的參數集已全部補記「示例」,依據句「D-038:未經用戶對齊」,逐列蓋章。

· 2026-08-30 14:34 核實(接手後重跑一次,不引上一手的結論):`backtest_run` 裡 origin='formal' 的 13 列——當中 12 條算數、1 條(`run-47479e5b36fed94a`)已由 KARST-093 除名——全部 13 個欄位**逐條逐格與動庫前的對照組比,對不上 0 格**。既有簽章 51832 列**一列都沒有消失、沒有改過**,現庫 51845 列,多出來的 13 列全部是 `param_set_alignment` 的新簽章。KARST-090 那份驗收原樣重跑(內容層真的重跑一次引擎),13 條全部 ✔,淨值/持倉/訂單逐位相同,落檔與 094 之前那份**逐字相同**。`karst verify` 三類全清白:定義 50826 列、因子批次 997 列、快照 22 列。

· 2026-08-30 14:34 測試:`tests/test_param_alignment.py` 與 `tests/test_factor_mix.py` 兩個所涉檔,**20 條全綠**。備份 `karst.sqlite.2026-08-30-094.bak` 已在動庫前做妥(核實過:第 14 版、兩張新表都未建,確是動庫前的樣本)。

做完,待收貨。

## 留言

### agent:agent-094b · 2026-08-30 14:35
接手:上一手(agent-094)做到最後核對階段因用量上限中斷,`claimedBy` 已由 agent-094 改為 agent-094b。它的成果全部留在工作樹上,我的職責是核實並補完,不是推倒重做。核實下來:五項要求上一手都已經做齊,程式那一邊沒有半成品——庫身新表與兩個 trigger、治理清單與報告分類兩份都登記、唯一入口 `params mark-alignment`、執行台登記時同步寫入、定義庫三個讀取介面、取數層 `paramSetAlignment`、生產庫 13 個參數集補記「示例」,連 CONTEXT.md 詞條都加了。

我補的三件:

一、**補回一支不存在的驗收腳本。** `tests/test_param_alignment.py` 的註釋指向 `experiments/2026-08-30-param-alignment/verify_094.py`,但那個檔從來沒有寫出來——即是說票上「13 條逐位不變」那句話,下一個人重現不到。已補寫(唯讀,兩個庫都以 mode=ro 開),並真的跑過一次。

二、**上一手用作證據的那個 sha256 重現不到,已改為逐格比。** `baseline_runs.json` 有個 `sha256` 欄,README 引它作「動庫前後同樣是 7607eb0c…」的證據,但計法沒有寫低;我試過 50 幾種序列化組合都砌不回同一個值。所以我不引它,改為把 `backtest_run` 全部 13 個欄位逐條逐格比——結果對不上 0 格。逐格比本來就比雜湊強:雜湊只答「一不一樣」,逐格比答得出哪一條哪一格不一樣。簽章那邊同樣改為全表逐列比(備份庫 vs 現庫),removed = 0、added = 13 列全屬 `param_set_alignment`。

三、**改正 README 兩處講錯的數。** (a)「13 條正式運行」——`origin='formal'` 是 13 **列**,但**算數的是 12 條**,`run-47479e5b36fed94a` 已由 KARST-093 除名(除名是加一列不是刪一列,所以它的列照舊在庫內)。逐格核的是那 13 列,即 12 條的超集,結論不受影響,但兩個數要分開講,否則下一個人會以為有人數錯。測試那句註釋亦一併改正。(b)「庫身由第 15 版升到第 16 版」——**生產庫實際是由第 14 版跳到第 16 版**。KARST-091 把程式常數升到 15,但生產庫在 094 之前一直沒有被新程式開過,`schema_meta` 仍停在 14、連 `sweep_batch` 那張表都未建;094 這次開庫兩張表一次過補建。DDL 全部 `CREATE TABLE IF NOT EXISTS`,既有登記一列不動——逐格比與簽章比就是這句話的證據。順帶記一筆:`sweep_batch` 補建後是 0 列,那屬 KARST-091 的帳,我沒有碰,亦不在本票範圍。

驗收數字:那 13 列正式運行逐格對不上 0 格;既有簽章 51832 列 0 列消失或被改,新增 13 列全屬對齊標記;KARST-090 那份驗收原樣重跑(內容層真的重跑一次引擎)結論不變,落檔與 094 之前逐字相同;`karst verify` 三類全清白(定義 50826、因子批次 997、快照 22);`tests/test_param_alignment.py` + `tests/test_factor_mix.py` 20 條全綠,只跑了這兩個所涉檔。備份核實過確是動庫前的樣本。

一件留給後續、不在本票的:票的第 (4) 項要求「策略頁批次門面與參數區能顯示」。取數層那一半已經做好(`RunReader` 每次運行帶出 `paramSetAlignment`,含 mark/label/alignedOn/basis/isAligned),命令列亦分得出三句話;但頁面上真的畫出來那一步屬 KARST-097(策略頁實作,D-047 之後押後),而且動畫面要先過設計那一關。本票不代它做。未 commit。
