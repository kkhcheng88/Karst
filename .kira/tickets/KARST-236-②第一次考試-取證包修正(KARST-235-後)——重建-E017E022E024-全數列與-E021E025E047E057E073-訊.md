---
id: KARST-236
title: ②第一次考試 取證包修正(KARST-235 後)——重建 E017/E022/E024 全數列與 E021/E025/E047/E057/E073 訊號季行:收入等四欄改用「最早申報」首報值(不按 tag 次序),訊號季行只放 EX-99.1 稿內數字(稿內無者查不到),同步修正 g0/prev_q_yoy/accel_pp;修 finlib 取值邏輯並對全部 84 包重跑完整性核查與遮罩;舊包備份;不改鎖定與其他包內容;不列公司名
type: research
createdAt: 2026-09-13
risk: high
model: opus
fits: KARST-235 核查結論(E022/E024 revenue 欄整欄向後錯位一年含 T1 後實際值;E017 營業利潤一格為 T1 後重述值;6 包訊號季行標稿內卻放 XBRL 值;成因 finlib.quarterly 跨 tag 取次序優先);考試有效性;D-175 資料修正交 DeepSeek
dependsOn: []
claimedBy: null
epic: 方法論期(D-166)
deliverable: KARST-D06
---

## 工作內容

先讀 A3/收入欄完整性核查——A3.md(成因與逐包表)與 A3/audit_revenue_integrity.py。第一步:修 A3 用的 finlib.quarterly(複製到 A3/finlib_fixed.py,不改 A/finlib.py):同一 period_end 多個收入 tag 時取「申報日最早」的值而非 tag 次序;財年末季推算照舊;每格記來源申報日。第二步:對 E017、E022、E024 用修正邏輯重建整個 4_財務數列(八季四欄 + 十項新欄),並核每格來源申報日 ≤ T1(訊號季除外);訊號季行 revenue/gross_profit/operating_income/ocf 一律改為 EX-99.1 稿內數字(數字匹配;稿內無者標查不到),revenue_source 標籤與值一致;重算 g0(稿內)、prev_q_yoy、accel_pp 並寫回 1_事件識別 與 4_財務數列(若重算後不再「加速」,記 entry_status=入口不成立,不換位,留給評分)。第三步:對 E021、E025、E047、E057、E073 只修訊號季行(值改稿內或查不到,標籤一致),其餘行不動。第四步:全部 84 包重跑 audit_revenue_integrity.py(修正版 finlib 作真值)與 masking 掃描,確認 leak_suspect = 0、標籤值一致 = 84;第五步:備份修改前的九包到 A3/cache/packets_before_fix235/;寫 A3/修正紀錄——235.md(逐包改了哪些格、修改前後值、g0/accel 變化、重跑結果);不改 picks_final 與其他 75 包的內容;不列公司名。含中文檔案只用 Read/Write/Edit;PYTHONUTF8=1;不 commit。

## 驗收條件

- [ ] E017/E022/E024 三包重建後,audit_revenue_integrity 四欄逐格相符且 leak_suspect = 0;反例:任何一格來源申報日 > T1(訊號季稿內值除外),即不合格
- [ ] 八包訊號季行 revenue_source 標籤與值一致(稿內數字或查不到);84 包重跑核查 leak_suspect = 0;masking 越界 0
- [ ] 修正紀錄逐包列前後值與 g0/accel 變化;舊包已備份;其餘 75 包 sha256 不變;不列公司名;不 commit

## 結果

## 留言
