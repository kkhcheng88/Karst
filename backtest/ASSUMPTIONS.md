# ASSUMPTIONS — 用戶假設憲章(任何實驗開跑前必引)

> **呢啲係用戶嘅風險決定,唔係實證結論。** 實驗唔准偷偷用返自己嘅預設(全歷史窗口、
> 入市/離場框架、alpha 判官)——2026-07-17 一日之內八次轉軚,**冇一次係數據變咗,
> 全部係框架靜靜哋換咗**。呢個檔就係框架嘅鎖。
>
> 引用方式:每個新 `exp_*.py` docstring 第一行寫「按 ASSUMPTIONS.md(2026-07-17 版)」。
> **唔引 = 唔准跑。改本檔 = 用戶決定,agent 只准提議。**

| # | 假設 | 內容 | 來源 |
|---|---|---|---|
| **A1** | **永遠喺市** | 決策係「**現貨定 LEAP 定混合**」,唔係「入定唔入」。任何擇時測試嘅基準 = **100% 現貨**,唔係現金。估錯嘅代價係「白俾租金」,唔係「踏空」 | 用戶 2026-07-17(第八次糾正) |
| **A2** | **尾部** | 接受「唔會再有 2000-2002 式兩年半唔彈嘅崩法」;可接受 MaxDD ≈ SPY 自身(~-34%)。**200SMA 唔做入場濾網**;趨勢閘只保留喺「槓桿防爆倉」一個用途 | 用戶 2026-07-17 裁決(SETTLED §8-A;性質 = 押注,唔係回測結論) |
| **A3** | **量度** | **資本效率(PnL÷曝險)+ 絕對 PnL 並列**;alpha 只准喺全組合層出現。CapEff 兩個陷阱(分母細谷高比率、蝕本組合排名倒轉)每表必檢 | 用戶明令 ×2(memory: metric-and-direction-discipline) |
| **A4** | **窗口** | 2016-01-01 起、前後半必分、**再加 regime 拆解**(平靜 vs 震盪)——RSI2 類訊號喺平靜市會死,唔拆開會出假陰性/假陽性 | 用戶明令 + `2026-07-17_rsi2_connors_settle.md` |
| **A5** | **成本** | LEAP 用 **m=1.15** 定價(07-09 實測 m=0.85 全面偏低;直到有真期權鏈);ETF 20bp 來回;期權 0.5%/side。**m=0.85 嘅結果只准做對照,唔准做判詞** | `2026-07-17_core_topup_realcost.md` |
| **A6** | **節奏** | 週/月級決策。唔做 day trade、唔做 0DTE | 用戶 |
| **A7** | **載具** | Tier-1/2 = SPY/QQQ(+ MAGS/SMH 兩端),**ETF-only**;個股只存在於 thesis 衛星(tier-3),而且受 sizing v2 管 | 用戶 |

## 判詞紀律(防轉軚機制,同日生效)

1. **Subagent 只准報數,唔准落判詞。** 判詞只可以喺覆核點、對照本憲章 + 預先登記嘅
   spec(問題/宇宙/窗口/基準/指標/門檻)落;**更新咗 `SETTLED.md` 先算數**。
2. **措辭分級**:「呢個配置下驗唔出」≠「死咗」。凡負面判詞冇對照臂(隨機同曝險/配對)= 無效。
3. **兩條機械閘**(由 false_negative_audit 起草,待接入 lint):
   - 開新 `exp_*.py` 前必 grep `SETTLED.md` + `backtest/results/` —— 有 hit 未讀 = 停;
   - results 檔含負面判詞關鍵詞但冇「對照」一節 = ERROR。
