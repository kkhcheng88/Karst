# 可由固定種子重生的實驗數據,不入 git
experiments/*/data/
# 大型原料不入 git;D-181:SQLite 的任務、游標、發布與模型帳本是持久狀態,不可當可重抓快取,須另備份;
# 說明檔的範本住在 karst/data/manifest.py(單一定義),每個快照目錄裡那份是照它填出來的實例。
# 只擋倉根那一個 data/,不要連 karst/data/(程式碼)一齊擋走
/data/
/karst.sqlite
/karst.sqlite-journal
# 唯一入口的簽章鑰匙(D-020 第 4 條):它刻意住在庫檔以外,拿得到庫檔不等於
# 簽得出章。入了 git 這條防線就沒了,故此永不入倉。
/karst.sqlite.gateway-key
# 板塊倍數版本存檔(KARST-124):存檔正本 readings.csv **要入 git**——它不可再生,
# 今日不抄以後補不回,只住在一部機上等於隨時歸零。擋走的只是原始頁面那一堆,
# 體積大而且可由正本的雜湊追溯。
# (vintage 存檔已於 2026-09-14 刪除)
# 插件工作目錄,不屬項目內容
.skill-compass/
experiments/*/.venv/
# 抓回來的原始 API 快取(KARST-136 前瞻預估):可重抓、體積大,不入 git
experiments/*/cache/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
/karst/web/screenshots/
# 2026-09-11: workers fetched raw filings into research dirs; raw docs live only in data/sec (D-134)
research/2026-09-methodology/*/docs/
research/2026-09-methodology/*/*/docs/
research/2026-09-methodology/*/edgar_cache/
research/2026-09-methodology/*/*/edgar_cache/
# 2026-09-12: 票 A 的中間快取含申報原文摘句(判指引正則的窗),同樣不入庫
research/2026-09-methodology/*/*/cache/

# reset 2026-09-13: tool caches
.pytest_cache/
.ruff_cache/
.skill-compass/
__pycache__/

# workbench v1 (2026-09-14): evidence layer and user inbox are not versioned
/evidence/*
!/evidence/README.md
!/evidence/manifest.jsonl
/inbox/*
!/inbox/README.md
!/inbox/links.md
/site/*.html

# D-181: runtime DB / journals / model ledger are durable, backed up outside git
/runtime/
