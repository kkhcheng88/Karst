# 因子輪動:四個驅動器的矩陣掃描(KARST-036)

用戶 2026-08-28 原話:

> I would like to know what is the best driver to move between 4 to get the best portfolio just
> with the 4 ... I don't mind overfitting at first

KARST-029 那次答的是「四格權重**固定**時哪一組最好」。這一次答的是下一個問題:
**權重不再固定,靠什麼訊號推動它在四隻之間移,最有效?**

這個目錄是那次掃描的產物。**只交表與判讀,不裁定哪一個驅動器該用**——那是用戶的
領域(D-008)。掃出來的最優**不是現役設定**:本目錄一句 `set_active_setup` 都沒有。

## 掃的是什麼

| 項目 | 內容 |
|---|---|
| 策略 | 因子輪動(ETF 版)第 1 版 |
| 持倉 | QUAL(質素)、VLUE(價值)、MTUM(動能)、USMV(低波)四隻因子 ETF |
| 訊號另用 | SPY——**只做訊號與基準,一股不持** |
| 基準 | SPY 與 QQQ |
| 期間 | 2015-01-02 ~ 2026-08-26,2,929 個交易日 |
| 數據快照 | `2026-08-28-000b4820a23a`(與 KARST-029 同一個;舊 `2026-08-27-91a5d51339d9`,見下「數據目錄重建」) |
| 引擎 | vectorbt 0.1.0 |
| 熱身期 | 頭 252 根 K 線四格各佔 25%,之後才由驅動器話事 |
| 主軸指標 | 對 SPY 的年化超額;另一份報告以最大回撤為主軸 |
| 外部數據 | **零**(見下面「外部數據」一節) |

期間、快照、基準、熱身期四樣**四個驅動器完全一樣**,所以格與格之間的差異只可能
來自驅動器與它的參數。

## 四個驅動器

驅動器(rotation driver)是策略層的可換件,四個走同一個接口:**收四隻 ETF 截至
決策日收工為止的收價,回四格權重**。換驅動器不改引擎、不改因子混合策略的目標比重
路徑——策略層照樣只砌一張「日期 × 實體編號 → 目標比重」的表交出去。

| 驅動器 | 訊號 | 掃描的參數 | 格數 |
|---|---|---|---|
| 因子動量排名 | 過去 L 個月報酬排名 | L = 1/3/6/9/12 月;`winner`(全押第一)或 `rank`(名次遞減 4:3:2:1) | 5 × 2 × 2 節奏 = **20** |
| 相對強弱對 SPY | 過去 L 個月報酬 vs SPY | L = 1/3/6/9/12 月;全輸時 `cash`(持現金)或 `equal`(等權) | 5 × 2 × 2 節奏 = **20** |
| 逆波幅 | 過去 L 日每日波幅的倒數 | L = 21/63/126/252 日;集中程度 1 或 2 次方 | 4 × 2 × 2 節奏 = **16** |
| 大市趨勢開關 | SPY 收價 vs M 日均線 | M = 50/100/150/200 日;押注比重 50% / 75% / 100% | 4 × 3 × 2 節奏 = **24** |

換倉節奏(月度、季度)四個驅動器都掃,合共 **80 格**,每格一個運行編號。

**相鄰的定義**(判平原、山脊與孤峰全靠它):笛卡兒積格上「相鄰」= **每條連續軸最多
移一步、且不可全部不動;選擇軸釘死不動**。選擇軸不入鄰域,而是把整個格切成**層**,
每層各出一份判讀。四個驅動器的軸型:因子動量與相對強弱各得一條連續軸(回望期),
`mode`／`fallback` 與節奏都是選擇軸,切出四層;逆波幅與大市趨勢開關各得兩條連續軸
(回望日數與集中程度、均線日數與押注比重),只有節奏是選擇軸,切出兩層。這條定義
印在每一份報告的頂頭。

## 成績表(對 SPY 年化超額,全期)

| 名稱 | 參數 | 年化超額 | 年化 | 最大回撤 | Sortino | 換手 | 裁決 | 對固定 95/5 | 對各佔 25% |
|---|---|---|---|---|---|---|---|---|---|
| 相對強弱·最優格 | L=6、cash、月度 | **+4.04%** | 17.89% | **−21.14%** | **1.24** | 3.23 | 孤峰 | **+2.83pp** | +4.70pp |
| 因子動量排名·最優格 | L=3、winner、月度 | **+3.24%** | 17.10% | −34.08% | 0.98 | 3.94 | 孤峰 | **+2.04pp** | +3.91pp |
| 因子動量排名·鄰域平均最高 | L=6、winner、月度 | +1.95% | 15.80% | −34.21% | 0.91 | 3.18 | 普通 | +0.74pp | +2.61pp |
| 逆波幅·最優格 | 252 日、1 次方、季度 | −0.85% | 13.00% | −34.66% | 0.76 | 0.13 | 普通 | −2.06pp | −0.19pp |
| 大市趨勢開關·最優格 | MA50、押 50%、月度 | −1.06% | 12.80% | −34.45% | 0.72 | 1.28 | 普通 | −2.26pp | −0.39pp |
| 大市趨勢開關·鄰域平均最高 | MA200、押 50%、月度 | −1.25% | 12.60% | −34.45% | 0.69 | 0.73 | 普通 | −2.46pp | −0.59pp |
| *對照* 固定 95/5(季度) | 動能 95%/價值 5% | +1.21% | 15.06% | −34.30% | 0.75 | 0.03 | — | 0 | +1.87pp |
| *對照* 各佔 25%(季度) | 四格均分 | −0.66% | 13.19% | −34.93% | 0.76 | 0.08 | — | −1.87pp | 0 |

八項指標齊全的版本在 `成績表.csv`(連同對 QQQ 的超額、勝率、盈虧比、平均持倉日與
每一行的運行編號)。

## 分段年化超額(對 SPY)

| 名稱 | 2015–2019 | 2020–2022 | 2023–2026 |
|---|---|---|---|
| 相對強弱·最優格 | +0.57% | **+12.47%** | +1.90% |
| 因子動量排名·最優格 | +2.42% | +0.42% | **+7.42%** |
| 因子動量排名·鄰域平均最高 | +0.70% | +0.61% | **+5.68%** |
| 逆波幅·最優格 | +0.51% | −1.53% | −2.10% |
| 大市趨勢開關·最優格 | +1.55% | −2.90% | −2.99% |
| 大市趨勢開關·鄰域平均最高 | +1.10% | −2.34% | −3.53% |
| *對照* 固定 95/5(季度) | +2.80% | −1.12% | +1.61% |
| *對照* 各佔 25%(季度) | +0.59% | −2.06% | −1.06% |

三段用的是**同一批運行**,只換檢視視窗重看一次(重看不重跑,規格 8.5);每一段的
SPY 按同一段日子另算一條買入持有線,兩邊比得過。

## 判讀:兩個贏家都是孤峰

| 驅動器 | 平原 | 孤峰 | 無效 | 最優格的鄰域平均 |
|---|---|---|---|---|
| 因子動量排名 | 0 | 4 | 0 | +1.68%(本格 +3.24%,落差 1.57pp) |
| 相對強弱對 SPY | 0 | 7 | 0 | +2.98%(本格 +4.04%,落差 1.05pp) |
| 逆波幅 | 0 | 0 | 0 | −1.16%(本格 −0.85%) |
| 大市趨勢開關 | 0 | 1 | 0 | −1.50%(本格 −1.06%) |

**「山脊」那一欄現時填不到,而我不會估。** `summary.json` 逐個驅動器的裁決分佈只報平原、
孤峰、無效三項;而各驅動器目錄下那份 `判讀表.csv` 裝的是**最大回撤**那一軸(旁邊另有
一份 `報告-最大回撤.md`),不是這裡用的年化超額軸。所以年化超額軸上有幾多格判山脊,
現時落不到檔——要數,就要重跑一次判讀並把該欄補入 `summary.json`。

按 D-016 第 3 條與 KARST-029 的判準,**兩個跑贏固定權重的驅動器,它們的最優格都判
孤峰**——峰值明顯高於四周,即那個數字有很大機會是擬合噪音而不是那組參數本身有效。
兩件事要一齊看:

- **因子動量排名**的最優格(L=3、全押第一、月度)是四個之中**抬升最大**的一個:高出
  自己那條回望期軸上的鄰域平均 1.57 個百分點。不過鄰域平均本身有 +1.68%——改一改回
  望期,超額會縮一大截,但不會歸零。而它的優勢**分佈在三段之中的兩段**(2020–2022
  與 2023–2026 都贏固定權重),不是靠單一個時期。
- **相對強弱對 SPY** 的最優格是四個驅動器之中,兩個「單點最優」與「鄰域平均最高」
  **同一格**的其中之一(另一個是逆波幅,但逆波幅全期是負超額);鄰域平均 +2.98% 亦是
  四個之中最高;它的最大回撤 −21.1% 比所有對照淺十三個百分點,Sortino 1.24 也是全場
  最高。但它的超額幾乎全部來自 **2020–2022 那一段(+12.47%)**——那正是它按參數走去
  持現金的那一段。換句話說,它贏的是一次股災,不是十一年。

**一個平原都沒有,要打個折來讀。** 平原的判準是「本格與鄰域平均雙雙排前一成」,
而這四個格只有 16 至 24 格(KARST-029 那次是 3,542 格),前一成即頭兩格,鄰域平均
要同時擠進頭兩格幾乎不可能。所以「0 平原」在這裡**不等於**四個驅動器全部不穩健,
它更多是格太細的後果。要判得準,下一步是把格加密(例如 L 逐月掃)。

## 外部數據

**一個都沒有用。** 四個驅動器的訊號全部由快照裡六隻 ETF 自身的收價算出來——沒有
VIX、沒有債息、沒有任何第三方序列,所以沒有來源要列、沒有費用要問、亦沒有知情
時間以外的處置要交代。這一點寫死在 `karst.strategies.factor_rotation.EXTERNAL_DATA`
(現時是空的),日後有驅動器非要外部序列不可,要先在那裡列明「來源、免費與否、
知情時間怎樣處置」才准實作,驗收測試盯住這一格。

知情時間本身照 D-021 第 3 條辦:驅動器收到的價格歷史由 `closes.loc[:決策日]` 切出來
——**最後一行就是決策日那一根**,之後的價格它連拿都拿不到;成交一律在下一根 K 線
的開價。這一條有專門的測試盯住(`tests/test_factor_rotation.py`)。

## 每個目錄裡有什麼

```
results/
├── 成績表.csv          每個驅動器最優格 + 四個固定權重對照,八項指標與差距
├── 分段超額.csv        三段 × 每一行的年化、SPY 年化、年化超額、最大回撤
├── summary.json        機讀摘要:格數、門檻、最優格、頭五格、成績表、分段
├── factor_momentum/    ─┐
├── relative_strength/   │ 每個驅動器一個目錄,各有:
├── inverse_volatility/  │   報告.md、報告-最大回撤.md
└── trend_switch/       ─┘   掃描表.csv、判讀表.csv、六張投影圖 PNG
```

圖例:▲ 孤峰、· 平原、\* 部分無效、斜線格 = 整格無效(成交太少)。

## 重跑

倉根目錄:

```
set PYTHONUTF8=1
python experiments/2026-08-28-factor-rotation-drivers/run_rotation_sweep.py
```

三個判讀門檻沒有預設值可言(寫在命令列,印在報告裡):`--min-trades` 成交少於這個
數判無效(用 30)、`--margin` 高出鄰域平均幾多才算孤峰(用 0.005)、`--quantile`
平原高地由哪一個分位起計(用 0.90)。`--warmup` 是熱身期根數(用 252),
`--drivers` 可以只跑其中幾個。

重跑一次不會重算:每一格的運行編號由「策略版本 × 參數集 × 期間 × 數據快照 × 引擎
版本」算出來,庫裡已經有那個編號就直接讀回。四個固定權重對照正是這樣來的——它們
是 KARST-029 已經跑過的運行,這次一格都沒有重跑。

數據快照與運行序列(`data/`)不入 git。

## 數據目錄重建(KARST-057,2026-08-28)

倉根 `data/` 2026-08-28 被誤清空(436 MB:運行序列與三份快照),`karst.sqlite` 完好,
沒有檔案系統層面的備份,用戶裁決當作**重建**。本目錄 80 格已按重抓的快照全部重跑。

| 項目 | 舊 | 新 |
|---|---|---|
| 數據快照 | `2026-08-27-91a5d51339d9` | `2026-08-28-000b4820a23a` |
| 逐格運行編號 | 全部改變 | 全部改變 |

運行編號由「策略版本 × 參數集 × 期間 × **數據快照** × 引擎版本」算出,而快照編號 =
抓取日 + 內容雜湊前 12 位,重抓必然換一個編號,80 格的運行編號因此全部跟住改。逐格
編號見 `results/summary.json` 與 `成績表.csv`。

**成績本身沒有改。** 四個驅動器的最優格一格都沒有換,數值對到小數點後第八位:

| 驅動器 | 最優格 | 舊 | 新 |
|---|---|---|---|
| 因子動量排名 | 回望 3 個月、winner、月度 | 0.032439113 | 0.032439112 |
| 相對強弱對 SPY | 回望 6 個月、持現金、月度 | 0.040373290 | 0.040373337 |
| 逆波幅 | 回望 252 日、次方 1.0、季度 | −0.008538501 | −0.008538524 |
| 大市趨勢開關 | 50 日均線、傾斜 0.5、月度 | −0.010552502 | −0.010552511 |

差異在小數點後第六位以下,來自 yfinance 在兩日之間修訂過幾根舊 K 線。逐條數字的新舊
對照見 [`../2026-08-28-rebuild/成績新舊對照.md`](../2026-08-28-rebuild/成績新舊對照.md)。

快照編號現時不寫死在腳本裡,七支成績腳本共用 `experiments/snapshot_ids.py` 一份;重抓
之後只改那一個檔。

**裁決欄與鄰域數字變了,但不是數據造成的。** 上面那張判讀表 08:27 落檔,而 KARST-047
新增「山脊」裁決、KARST-048 把軸型宣告(連續軸走鄰域、選擇軸切層分判)接入生產掃描
路徑,是同日 10:01 與 10:26 的事——舊表產生時根本沒有「山脊」這一個裁決,鄰域亦未按
軸型收窄。今次重跑用的是現行規則,上文已按新規則重出。變的是這幾樣:

- **孤峰格數**由 2/1/0/2 變成 4/7/0/1(順序同上表):鄰域收窄之後,一格要贏過的鄰居
  少了,「四周最高」自然更容易成立。
- **大市趨勢開關的最優格由孤峰變普通**:它原本那個孤峰,是靠「換一個節奏」那批鄰居
  襯出來的。
- **「鄰域平均最高」搬了格**:因子動量由「L=6、按名次分注」變成「L=6、整注押第一」;
  大市趨勢開關由與最優格同一格,變成 MA200、押 50%、月度——所以成績表與分段表各多
  了一行。
- **最優格的鄰域平均全部上升**(例如因子動量由 −0.17% 變 +1.68%),因為把平均拉低的
  別層鄰居剔了出去。

**逐格成績本身一個字都沒有動**(見上表,差異在小數點後第六位以下);逐格裁決一律以
`results/summary.json` 為準。

## 能力本體在哪

| 檔 | 內容 |
|---|---|
| `karst/strategies/factor_rotation.py` | 驅動器接口、四個驅動器、目標比重路徑、跑一次回測 |
| `karst/sweep/factor_rotation.py` | 掃描接法:參數格、一格怎樣跑、成績表、分段超額 |
| `tests/test_factor_rotation.py` | 四條驗收條件各一個測試(離線合成價格) |

掃描四層(格 / 逐格跑 / 判讀 / 報告)本身住在 `karst/sweep/`,本票一個字都沒有改
它——因子輪動只是接上去的第二套策略,證明那四層真的與策略無關。
