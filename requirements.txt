# Deployed service (see zeabur.md). Versions are the ones actually run locally.
# Credentials never live here; only environment variable NAMES appear in docs.
# GitHub OAuth mode (karst/auth.py) needs nothing extra: the OAuth proxy, its disk
# store (py-key-value-aio[disk]) and cryptography all arrive with fastmcp.
fastmcp==2.14.7
mcp==1.27.0
longport==4.3.7
jsonschema==4.26.0
referencing==0.37.0
rfc3339-validator==0.1.4
tzdata==2025.3
httpx==0.28.1
anthropic==0.105.2
# Transcripts / quarterly statements adapter; pulls duckdb + pandas + pyarrow.
defeatbeta-api==0.0.61
# Standard day/week/month charts for the researcher (karst/charts.py). Agg backend
# only: no GUI, no display, nothing interactive on a server.
matplotlib==3.10.9
