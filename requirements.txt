# Karst dashboard — pinned to the locally-validated stack (Python 3.12).
# The live scan needs only these + network (defeatbeta/HuggingFace, SEC EDGAR);
# the 200MB backtest price caches are NOT required to serve the dashboard.
numpy==2.4.4
pandas==3.0.1
scipy==1.17.1
scikit-learn==1.8.0
yfinance==1.3.0
PyYAML==6.0.3
defeatbeta-api==0.0.60
duckdb==1.5.3
flask==3.1.3
gunicorn==23.0.0
